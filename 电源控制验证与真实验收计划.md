# 电源控制验证与真实验收计划

> 文档性质：电源控制专项唯一执行入口。
>
> 当前状态：P-1 至 P-6 已完成离线仿真验证；P-7 真实电源空载/电子负载验收待现场条件确认；P-8 等待 DUT 和驱动功放到位。
>
> 安全原则：仿真测试不能替代真实硬件验收；没有确认设备、通道、负载和手动断电方式前，不执行真实输出。

## 1. 当前入口和边界

后续所有电源控制工作只维护本文，不再拆分为仿真、专项入口和真实验收三个文档。

当前禁止：

- 不运行 `instrument_control.py` 的综合 `main()`。
- 不连接驱动功放和 DUT。
- 不执行完整功率扫描。
- 不开启 RF。
- 不把仿真结果当作真实硬件安全证明。

## 2. 已完成离线里程碑

### P-1：依赖注入

- `InstrumentControl` 支持注入 `resource_manager` 和 `sleep_fn`。
- 保留 `InstrumentControl()` 和 `InstrumentControl(config_path)` 旧调用方式。
- 初始化失败时能够清理已创建的资源。

### P-2：Fake VISA 资源

- `tests/fakes.py` 提供 `FakeVisaResource` 和 `FakeResourceManager`。
- 支持写入、查询、打开、关闭命令记录。
- 支持跨操作时序记录。
- 支持写入、查询、打开和关闭故障注入。

### P-3：基础电源命令

已验证：

- 电压和电流设置。
- 过压和过流保护状态和值。
- 电压和电流查询。
- 通道 ON/OFF。
- 未知电源、无效读数和底层故障传播。
- 初始化时保护状态先于保护值设置。

### P-4：上下电顺序基线

当前旧逻辑基线：

```text
上电：CH1 ON -> 等待 -> CH2 ON
掉电：CH2 OFF -> 等待 -> CH1 OFF
```

当前基线假设 `CH1` 为栅极、`CH2` 为漏极，只用于回归对照，不代表最终动态配置设计。

### P-5：异常和安全清理

- 新增 `safe_shutdown()`。
- 掉电某通道失败后继续尝试其他通道。
- RF 关闭失败后继续执行电源和连接清理。
- 资源关闭失败后继续处理剩余资源并汇总错误。
- 纯电源场景没有信号源时可以安全关闭。
- 避免重复发送 RF OFF。

### P-6：仿真验收

已覆盖：

- 正常清理。
- 上电中断。
- 漏极和栅极掉电失败。
- RF 关闭失败。
- 电源和 ResourceManager 关闭失败。
- 重复安全关闭。
- 不创建真实 VISA 连接。

## 3. 离线验证记录

最近一次使用 `Auto_test` 环境的结果：

```text
完整测试：95 项通过
GUI 导入测试：通过
编译检查：通过
git diff --check：通过
```

相关测试文件：

```text
tests/fakes.py
tests/test_fakes.py
tests/test_instrument_control_injection.py
tests/test_instrument_power_control.py
```

## 4. P-7：真实电源空载或电子负载验收

只有 P-6 通过后才执行 P-7。P-7 不能证明 DUT 或驱动功放负载安全，只验证电源设备控制、读数、输出开关和现场命令兼容性。

### 4.1 操作者先提供的信息

- [ ] 电源品牌、具体型号和固件版本。
- [ ] 当前实际 VISA 地址。
- [ ] 当前可用通道列表。
- [ ] 是否允许空载输出。
- [ ] 如果不允许空载，电子负载型号和连接方式。
- [ ] 允许的最低风险电压、电流和保护值。
- [ ] 现场手动关闭全部输出的方法。
- [ ] 操作者姓名和测试时间。

如果不能确认设备是否允许空载，默认按“不允许空载”处理，使用合适的电子负载或暂停真实输出。

### 4.2 环境和版本检查

在项目根目录执行：

```powershell
conda activate Auto_test
python --version
python launcher.py --check
python launcher.py --validate-config
```

记录：

```text
Python 版本：
PyVISA 版本：
项目提交号：
电源型号：
电源固件：
VISA 地址标识：
```

`--validate-config` 只校验旧配置，不代表本次运行映射已完成，也不替代现场通道确认。

### 4.3 现场隔离和进入门槛

- [ ] 不连接 DUT。
- [ ] 不连接驱动功放。
- [ ] 断开信号源 RF 链路，或确认 RF 输出保持关闭。
- [ ] 不运行 `instrument_control.py` 的综合 `main()`。
- [ ] 使用独立的真实电源测试入口。
- [ ] 现场安排操作者观察电源面板。
- [ ] 手动断电方式位于操作者可及范围内。

以下任一项未满足，不得进行真实输出：

- 电源型号或 VISA 地址不明确。
- 不清楚是否允许空载。
- 设备不允许空载但没有电子负载。
- 没有手动断电方式。
- 测试脚本可能打开 RF 或运行完整测量。
- 操作者无法持续观察设备状态。

### 4.4 连接和身份确认

目标：只连接电源，不打开输出。

1. 关闭电源所有输出。
2. 启动独立电源测试入口。
3. 通过当前 VISA 地址连接电源。
4. 查询设备身份信息，具体命令以设备手册为准。
5. 确认可用通道。
6. 读取各通道初始输出状态。
7. 读取各通道初始电压和电流。
8. 记录设备实际响应格式。
9. 不执行任何 `OUTPut ... ON`。

记录：

```text
连接时间：
操作者：
设备型号：
身份查询响应：
通道列表：
初始输出状态：
初始电压：
初始电流：
查询命令差异：
```

连接、身份或查询失败时保持输出关闭，记录错误并手动确认面板状态。

### 4.5 低风险参数设置

对一个明确通道执行：

1. 设置现场批准的低风险电压。
2. 设置保守电流限制。
3. 设置过压保护状态和值。
4. 设置过流保护状态和值。
5. 读取或通过面板确认设置值。
6. 确认输出仍为 OFF。

要求：

- [ ] 每次只操作一个通道。
- [ ] 电压和电流在现场批准范围内。
- [ ] 保护值单位和语义已确认，或明确记录为待确认。
- [ ] 设置参数不会自动打开输出。
- [ ] 不把现场测试值写回长期模板或 `config.json`。

任何设置或读回失败时，不得继续打开输出；应手动确认状态并断开连接。

### 4.6 单通道输出开关

前置条件：确认允许空载，或已连接额定匹配的电子负载。

1. 手动确认输出初始为 OFF。
2. 打开一个通道。
3. 观察面板并读取输出状态。
4. 读取实际电压和电流。
5. 关闭该通道。
6. 确认输出回到 OFF。
7. 重复一次，确认操作可重复。

验收标准：

- 输出状态与命令一致。
- 实际电压在允许误差范围内。
- 无异常电流或保护触发。
- OFF 后输出确实关闭。
- 重复操作不会改变保护设置或连接状态。

### 4.7 多通道确认

- [ ] 逐个确认实际可用通道名称。
- [ ] 记录通道数量和编号。
- [ ] 记录通道是否共享全局输出开关。
- [ ] 记录输出状态查询是否按通道生效。
- [ ] 记录电压、电流查询是否按通道生效。
- [ ] 不将通道自动命名为 `gate` 或 `drain`。

### 4.8 真实异常清理

可以测试：

- [ ] 输出打开后正常停止。
- [ ] 输出关闭失败后继续尝试其他通道。
- [ ] VISA 连接关闭失败时报告错误。
- [ ] 手动中断后确认面板输出状态。
- [ ] 重新连接后确认没有遗留 ON 输出。

禁止：

- 不故意制造过压或过流。
- 不在空载条件下故意触发保护。
- 不在输出开启时改变未知电压或电流参数。

### 4.9 P-7 记录模板

```text
日期：
操作者：
项目提交号：
Python/Conda 环境：Auto_test
Python 版本：
PyVISA 版本：
电源品牌/型号：
固件版本：
VISA 地址标识：
负载类型：空载 / 电子负载
实际通道列表：
设置电压：
设置电流：
保护值：
初始读数：
设置后读数：
输出开关结果：
上电顺序：未执行 / 通过 / 失败
掉电顺序：未执行 / 通过 / 失败
异常清理：未执行 / 通过 / 失败
全部输出关闭确认：
SCPI 差异：
设备异常：
手动断电记录：
回滚结论：
操作者确认：
```

### 4.10 P-7 失败和回滚

出现以下任意情况立即停止：

- 输出状态无法确认。
- OFF 后输出仍为 ON。
- 实际电压明显偏离设置值。
- 电流异常或设备保护触发。
- VISA 断开后输出状态未知。
- 脚本异常后无法证明全部输出已关闭。
- 设备命令与手册或面板行为不一致。

回滚动作：

1. 使用面板关闭全部输出。
2. 断开电源连接。
3. 停止使用新的真实测试入口。
4. 保留命令日志和验收记录。
5. 恢复旧入口。
6. 将问题记录到 `重构执行.md` 风险登记区。

## 5. P-8：DUT/驱放到位后的真实验收

P-8 必须在 P-7 通过且实际负载到位后执行。

### 5.1 运行映射和接线

- [ ] 记录本次信号源地址。
- [ ] 记录本次频谱仪地址。
- [ ] 记录本次电源地址。
- [ ] 记录驱动功放电源通道。
- [ ] 记录 DUT 电源通道。
- [ ] 记录稳定角色到现场通道映射。
- [ ] 确认 DUT 和驱动通道不重复。
- [ ] 现场操作者确认接线。
- [ ] 将运行映射保存为本次运行快照。

### 5.2 低风险上电

- [ ] RF 保持关闭。
- [ ] 设置并确认各通道工作参数。
- [ ] 先栅后漏上电。
- [ ] 观察各通道实际电压和电流。
- [ ] 确认没有异常电流或保护触发。
- [ ] 任何一步异常都执行 `safe_shutdown()` 并手动确认。

### 5.3 低功率 RF 验收

- [ ] 上电稳定后仍保持 RF 关闭。
- [ ] 从低功率、单频点开始。
- [ ] 确认 DUT 输入功率保护值。
- [ ] 只执行极少扫描点。
- [ ] 记录输入、输出、DC 电压、电流和效率读数。
- [ ] 用户停止后确认 RF 和电源均关闭。

### 5.4 异常和窗口关闭

- [ ] 测量异常后 RF 关闭。
- [ ] 测量异常后电源按反向顺序关闭。
- [ ] 用户停止后资源清理完成。
- [ ] GUI 正常关闭后资源清理完成。
- [ ] 关闭窗口时没有遗留输出。
- [ ] 结果保存失败时仍执行安全关闭。

### 5.5 P-8 禁止事项

- [ ] 未确认接线时禁止上电。
- [ ] 未确认 `max_input_power` 时禁止开启 RF。
- [ ] 不将旧 `CH1/CH2` 假定为当前 gate/drain。
- [ ] 不使用旧配置地址覆盖当次现场映射。
- [ ] 不因仿真通过而跳过低功率硬件验收。

### 5.6 P-8 记录模板

```text
日期：
操作者：
项目提交号：
运行 ID：
测试方案版本：
信号源型号/地址：
频谱仪型号/地址：
电源型号/地址：
驱动功放通道：
DUT 通道：
gate/drain 角色依据：
现场接线确认：
电压/电流/保护值：
RF 默认状态：
上电顺序：
低功率测试范围：
max_input_power：
停止行为：
异常清理：
窗口关闭清理：
结果快照路径：
结果差异：
是否回滚：
操作者确认：
```

## 6. 完成门槛

### P-7 完成

- [ ] 设备连接和身份确认通过。
- [ ] 逐通道查询通过。
- [ ] 低风险设置和读回通过。
- [ ] 单通道 OFF/ON/OFF 通过。
- [ ] 全部输出关闭状态已由面板或查询确认。
- [ ] 真实命令差异已记录。
- [ ] 没有 DUT/驱放时不宣称负载安全验收通过。
- [ ] 现场记录完整并可回滚。

### P-8 完成

- [ ] 运行映射和现场接线确认已保存。
- [ ] 低风险上电通过。
- [ ] RF 默认关闭和安全清理通过。
- [ ] 低功率单频点测试通过。
- [ ] 停止、异常和窗口关闭清理通过。
- [ ] 结果与运行快照一致。
- [ ] 真实验收记录包含操作者确认。

## 7. 验证命令

代码变更或真实测试记录提交前执行：

```powershell
conda activate Auto_test
python -m unittest discover -s tests -v
python -m compileall -q -x "__pycache__|test_results" .
git diff --check
```

真实设备验收必须附设备、通道、命令/响应、输出状态、异常清理、回滚结论和操作者确认。真实硬件验收不能由编译、单元测试或仿真结果替代。
