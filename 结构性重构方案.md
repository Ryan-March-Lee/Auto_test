# PA 自动测试系统结构性重构方案

> 文档性质：结构性重构的实施说明。
>
> 上位文档：`重构基线.md`（目标架构与不可破坏契约）、`重构执行.md`（阶段任务与回滚要求）。
>
> 适用范围：PA 自动测试主系统。`DPD_auto_test/` 暂不纳入本轮主链路重构。
>
> 当前状态：准备开始阶段 1。阶段 0.1 已完成文件整理，但现有收集结果不是同一次运行的完整基线，不作为测量数值回归基线。

## 1. 重构目标

本轮重构的目标是降低模块耦合、固定运行边界，并使核心测量逻辑能够在没有 GUI 和真实仪器的情况下验证。

目标结果：

1. GUI、CLI 和未来的其他入口共用同一套应用服务。
2. 测量服务依赖仪器接口，不直接依赖 PyVISA、Qt 或具体文件路径。
3. 一次运行拥有唯一的 `run_id`，方案、现场资源、快照、结果和报告可相互追溯。
4. 测量结果使用明确、版本化的结构，减少隐式字典键带来的行为漂移。
5. 普通停止、紧急停止、异常和窗口关闭都遵守统一的硬件清理规则。
6. 新实现可以通过仿真和旧实现对照，出现问题时能够切回旧入口。

本轮不以目录搬迁、类名调整或代码行数减少作为完成标准。完成标准是边界清晰、行为可验证、迁移可回退。

## 2. 当前认识与前置条件

当前主要问题：

- `enhanced_main_gui.py` 同时承担界面、配置、线程、测量启动、结果读写和可视化协调。
- `enhanced_workers.py` 通过继承同步测量类复制和改写测量流程，存在行为漂移风险。
- `cable_loss_measurement.py`、`driver_power_mapping.py` 和 `amplifier_measurement.py` 混合了业务编排、仪器操作和结果保存。
- `instrument_control.py` 同时处理 VISA 连接、SCPI 命令、电源安全顺序和部分运行配置。
- `result_storage.py`、`result_reading.py`、生命周期和配置模型已经存在，但尚未完全成为生产主链路。
- `collect_baseline.py` 当前对每类文件独立寻找最新文件，可能把不同运行的结果拼到同一份归档中。

现有 `baseline/collected/20260925-203233-586899/` 只能作为历史文件结构样例。正式测量回归前，必须重新生成同一个 `run_id` 下的完整运行快照和结果。

## 3. 不可改变的业务契约

在真实设备验收前，结构性重构不得同时改变以下内容：

1. 线损两步路径、衰减器补偿公式和结果字段语义。
2. 驱动映射的扫描范围、输入/输出关系、线损补偿位置和按频率保存方式。
3. 主功放有驱动/无驱动模式、扫描范围、压缩点、DC 功耗和效率计算。
4. DUT 最大输入功率保护触发后，不得继续设置更高激励。
5. RF 默认关闭；正常完成、停止、异常和关闭窗口均必须执行清理。
6. 电源上电遵守先栅后漏，掉电遵守先漏后栅。
7. 旧结果在迁移期间可读，旧启动入口在过渡期可回滚。
8. 每次运行保存实际方案、资源映射、软件版本、状态和异常信息。

公式、SCPI 命令、安全顺序和结果格式不得在同一个变更中一起修改。

## 4. 目标依赖方向

```text
Qt GUI / CLI
    -> Application services
        -> Domain models, calculations, safety rules
            -> Instrument ports -> VISA / simulation adapters
            -> Persistence ports -> JSON / files
            -> Analysis ports -> plots / reports / exports
```

依赖规则：

- `domain` 不导入 PySide6、PyVISA 或具体文件存储模块。
- 测量服务不读取 GUI 控件、不发送 Qt Signal、不拼接项目级固定路径。
- Qt Worker 只负责线程生命周期、取消请求和事件转发。
- VISA/SCPI 命令只存在于仪器适配器中。
- 结果和快照通过结果仓储写入，报告通过结果读取接口消费。
- AI、联网搜索、格式转换和 DPD 不得进入主测量初始化路径。

## 5. 核心对象

### 5.1 `TestPlan`

表示稳定的测试方法和参数：频率、功率扫描、压缩点、衰减补偿和保护值。它不包含现场设备地址，也不绑定 `PS4`、`CH1` 等现场名称。

### 5.2 `RunContext`

表示一次现场运行的资源：

- `run_id`
- 开始时间、操作者和软件版本
- 信号源、频谱仪和电源的设备标识
- 电源物理通道与供电角色的映射
- 接线确认和现场备注

### 5.3 `MeasurementSession`

表示一次运行的生命周期：状态、停止原因、异常、开始/结束时间和事件流。

### 5.4 `MeasurementResult`

表示测量输出，至少区分：

- 原始读数
- 补偿后的读数
- 派生指标
- 单位
- 方法版本和 schema 版本
- 关联的 `run_id`、方案快照和资源映射快照

正式运行链路必须是：

```text
加载 TestPlan
    -> 选择或填写 RunContext
    -> 离线校验
    -> 创建运行目录和快照
    -> 创建 InstrumentSession
    -> 执行测量
    -> 保存结果
    -> 生成报告
    -> RF 关闭、电源关闭、连接关闭
```

快照写入失败时，不得连接仪器、上电或打开 RF。

## 6. 分阶段实施计划

### 阶段 1：接口和协议冻结

目标：增加边界，不切换生产测量实现。

建议新增或整理：

- `instrument/ports.py`
- `app/events.py`
- `app/cancellation.py`
- 领域结果模型和运行上下文模型

任务：

1. 定义 `SignalGeneratorPort`、`SpectrumAnalyzerPort`、`PowerSupplyPort` 和 `InstrumentSession`。
2. 定义进度、消息、实时数据、现场确认、完成、停止和失败事件。
3. 定义普通停止与紧急停止的语义。
4. 明确每个仪器操作的单位、前置条件、超时和失败行为。
5. 将 `run_id`、方法版本和 schema 版本加入核心对象设计。

验收：

- 领域和接口模块可在无 PySide6、无 PyVISA 环境中导入。
- 现有生产入口和现有测量类行为不变。
- 新接口有纯 Python 单元测试。

回滚：删除或保留新增接口模块，不替换旧调用者。

### 阶段 2：仿真仪器和安全生命周期

目标：在无真实设备情况下验证硬件流程和清理行为。

任务：

1. 实现信号源、频谱分析仪和电源仿真器。
2. 记录设置、查询、RF 和电源命令。
3. 支持连接失败、设置失败、读数失败和清理失败注入。
4. 统一 `created -> validated -> connected -> prepared -> powered -> measuring` 状态。
5. 统一 RF 关闭、电源关闭和连接关闭顺序，并保证清理幂等。

验收：

- 覆盖正常完成、普通停止、紧急停止、异常和窗口关闭。
- 命令记录证明 RF 默认关闭以及电源上电/掉电顺序正确。
- DUT 输入保护触发后不再提高激励。

回滚：通过功能开关切回旧 `InstrumentControl` 和旧测量入口。

### 阶段 3：统一测量服务

目标：消除同步测量类和 Qt Worker 中的核心逻辑复制。

任务：

1. 抽取 `CableLossService`。
2. 抽取 `DriverPowerMappingService`。
3. 抽取 `AmplifierMeasurementService`。
4. 三个服务均只依赖方案、仪器端口、事件接收器、取消令牌和结果仓储。
5. 同步入口和 Qt Worker 先通过兼容包装调用新服务。
6. 保留已有纯计算函数作为唯一计算实现。

验收：

- CLI 和 Qt Worker 调用同一套测量服务。
- 新旧实现使用相同仿真输入时，关键结果一致。
- 三类服务都覆盖停止、读数失败和异常清理。
- 服务模块不导入 PySide6。

回滚：切换回旧测量类和 `enhanced_workers.py`。

### 阶段 4：配置与运行上下文迁移

目标：让 `TestPlan`、`RunContext` 和 `MeasurementSession` 成为内部主契约。

任务：

1. 旧 `config.json` 通过转换器进入新模型。
2. 新运行开始前创建唯一运行目录。
3. 在连接仪器前保存方案快照、资源映射快照、运行元数据和版本信息。
4. 所有测量结果、报告和日志引用同一个 `run_id`。
5. 改造 `collect_baseline.py`：按指定 `run_id` 收集完整运行，不再跨运行独立挑选最新文件。
6. 缺少关联快照或关键文件时，标记为不完整，不宣称为有效基线。

验收：

- 一个运行目录能够独立解释本次运行。
- 结果、报告、配置和快照的 `run_id` 一致。
- 旧配置仍可通过 `--validate-config` 检查。
- 运行目录冲突时不覆盖既有数据。

### 阶段 5：结果模型和分析解耦

目标：让结果格式稳定，让报告和绘图不依赖测量过程内部字典。

任务：

1. 为线损、驱动映射和主功放结果定义版本化结构。
2. 明确频率、功率、电压、电流、效率和压缩点单位。
3. 保留旧 JSON 只读适配器。
4. 分离结果读取、图表生成、CSV 导出和 HTML/PDF 组装。
5. 报告从结果模型读取，不直接访问测量对象。

验收：

- 现有历史样例和新格式结果均可读取。
- 新旧结果均可生成报告。
- 缺少压缩点、DC 数据或图形字体时有明确可诊断行为。

### 阶段 6：GUI 分层和入口收敛

目标：降低 `enhanced_main_gui.py` 的职责和修改风险。

任务：

1. 保留 `launcher.py` 为目标生产入口。
2. `start_gui.bat` 和 `vscode_launcher.py` 只做兼容转发。
3. Qt Worker 只处理线程、信号和取消。
4. 页面只负责输入、确认、状态展示和事件订阅。
5. 页面不直接创建仪器、不调用 SCPI、不读写结果文件。
6. 按配置、线损、驱动映射、主功放、可视化和导出拆分页面职责。

验收：

- 无真实仪器和无 AI 服务时 GUI 可以启动。
- 页面测试不要求 PyVISA 连接。
- Qt 与 CLI 的结果和清理行为一致。
- 正常关闭窗口不遗留线程、RF 或电源状态。

### 阶段 7：可选能力隔离与清理

任务：

1. 将 AI、联网搜索、聊天历史和格式转换移入可选能力边界。
2. 明确 `DPD_auto_test/` 的独立输入输出和依赖，不进行无关迁移。
3. 统计旧入口、兼容字段、旧类和旧结果适配器的调用者。
4. 经过至少一个稳定生产周期后，才删除无调用者的过渡实现。

## 7. 每个重构步骤的固定流程

每次提交都按以下顺序执行：

1. 明确本次只改变一个结构边界。
2. 先增加或调整测试，再修改生产代码。
3. 运行编译检查、单元测试和受影响模块测试。
4. 用仿真输入与旧实现对照关键结果。
5. 检查异常、停止和清理路径。
6. 记录文件、行为变化、验证命令和回滚方式。
7. 通过后再进入下一步，不把多个高风险步骤合并。

推荐验证命令：

```powershell
./run_tests.ps1
& "C:\My_Document\Anaconda\envs\Auto_test\python.exe" launcher.py --check
& "C:\My_Document\Anaconda\envs\Auto_test\python.exe" launcher.py --validate-config
```

涉及真实设备时，还必须记录：设备型号、地址标识、使用通道、接线确认、功率范围、停止/清理结果和关键结果差异。未完成真实设备验收前，不得删除旧实现。

## 8. 阶段完成门槛

一个阶段只有同时满足以下条件才算完成：

- 自动化测试通过。
- 既有启动入口可用，或已记录明确的兼容变化。
- 关键业务契约没有无意变化。
- 仿真场景覆盖正常、停止和异常路径。
- 相关结果仍可读，报告仍可生成。
- 涉及硬件时已完成必要的真实设备验收。
- 有明确回滚入口或旧实现仍保留。

出现以下任一情况，应停止后续重构并回滚当前切换：测量值异常、DUT 保护失效、RF 或电源未关闭、结果不可读、报告无法生成、GUI 无法启动、线程无法退出。

## 9. 重构记录模板

```text
日期：
阶段/步骤：
提交：
涉及文件：
结构边界变化：
行为变化：
验证命令：
仿真/自动化结果：
真实设备验收：未执行 / 通过 / 失败
结果差异：
回滚方式：
风险登记更新：
```

## 10. 第一批实施任务

建议从以下小步骤开始：

1. 盘点现有测量入口、Worker、结果写入点和仪器创建点，形成调用关系记录。
2. 增加接口和事件协议模块，不替换现有调用者。
3. 增加 `RunContext`、`run_id` 和运行目录的最小模型测试。
4. 实现最小仿真仪器和命令记录。
5. 将一个最小线损流程接入仿真服务，先验证停止和清理。
6. 通过同样方式接入驱动映射和主功放流程。
7. 最后再迁移 GUI Worker 和生产入口。

第一批不应同时修改测量公式、SCPI 命令、电源安全顺序、历史 JSON 格式或 GUI 布局。
