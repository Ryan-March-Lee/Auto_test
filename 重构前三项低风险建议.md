# PA 自动测试系统：前三项低风险重构建议

> 文档性质：重构调研与执行参考。
>
> 更新日期：2026-08-21
>
> 当前状态：第一项、第二项和第三项的代码重构及自动化/仿真验证已完成；真实设备验收待执行。

## 1. 总体约束

前三项重构以保持现有行为为首要目标：

- 不迁移 `config.json`、历史结果 JSON 或文件路径。
- 不改变现有类名、公有方法签名、GUI 操作流程和调用入口。
- 不改变 SCPI 命令文本、命令顺序、电源安全顺序、测量公式和结果字段。
- 优先采用私有方法提取或纯函数提取，不引入新的硬件抽象层。
- 使用 fake 控件、fake VISA 资源和行为快照验证重构前后一致性。
- 现场配置、结果文件和日志不是重构工具的自动覆盖对象。

暂缓以下高风险工作：

1. 测试方案与单次运行配置的正式切换。
2. 旧配置或历史结果 JSON 的破坏性迁移。
3. 同步测量类与增强工作线程的核心逻辑合并。
4. PyVISA 端口、仪器身份查询和硬件状态机重写。
5. 整体迁移 `enhanced_main_gui.py` 的目录结构。
6. 删除旧入口、旧字段或兼容层。

## 2. 优先级与状态

| 排名 | 重构点 | 风险 | 状态 |
| --- | --- | --- | --- |
| 1 | 拆分 GUI 配置读写与 UI 同步逻辑 | 低至中 | 已完成 |
| 2 | 拆分仪器初始化和电源安全流程 | 低 | 已完成 |
| 3 | 提取测量纯计算函数 | 中低 | 代码重构及自动化/仿真验证已完成；真实设备验收待执行 |

## 3. 已完成项摘要

### 3.1 第一项：GUI 配置读写与 UI 同步逻辑

已在 `enhanced_main_gui.py` 中完成方法级拆分，保留原有 GUI 入口和配置格式：

- `_read_instrument_config_from_ui()`
- `_read_test_parameters_from_ui()`
- `_read_power_supply_config_from_ui()`
- `_read_power_assignment_from_ui()`
- `_build_config_from_ui()`
- `_save_config_file()`

以下行为已锁定，不应在后续第三项中顺带修改：

- `config.json` 结构、路径和旧键名保持不变。
- `carrier`、`peaking`、`peaking2`、`CH1`、`CH2` 等旧语义保持不变。
- `_build_config_from_ui()` 基于深拷贝组装配置，不反向修改原始配置。
- UI 未提供的额外电源通道继续保留。
- `save_config()` 与 `update_and_save_config()` 继续复用统一保存方法。
- 不在后续重构中修复 `freq_edit` 的 `eval` 解析问题；该问题单独立项。

相关测试：`tests/test_gui_config_readers.py`，包含配置读取、组装和保存测试。

### 3.2 第二项：仪器初始化和电源安全流程

已在 `instrument_control.py` 中完成阶段方法提取，公有入口保持不变。

初始化阶段：

- `_initialize_signal_generator_if_enabled()`
- `_initialize_spectrum_analyzer_if_enabled()`
- `_initialize_enabled_power_supplies()`

上电阶段：

- `_power_on_gate_channels()`：所有 CH1，单通道间隔 0.5 秒。
- `_power_on_drain_channels()`：所有 CH2，单通道间隔 0.5 秒。
- CH1 与 CH2 阶段之间等待 1.5 秒。

掉电阶段：

- `_power_off_drain_channels()`：反向关闭所有 CH2，失败收集后继续。
- `_power_off_gate_channels()`：反向关闭所有 CH1，失败收集后继续。
- CH2 与 CH1 阶段之间等待 2 秒。

安全关闭仍保持：

```text
关闭 RF -> 执行掉电序列 -> 关闭 VISA 资源
```

已验证并必须保持的行为：

- SCPI 命令文本和命令顺序不变。
- 保护设置仍先设置保护状态，再设置启用的保护值。
- 初始化失败后已打开资源仍执行清理。
- 掉电单通道失败不会阻止其他通道和后续阶段。
- `safe_shutdown()` 在 RF、掉电或资源关闭失败时仍继续后续清理并汇总错误。
- `InstrumentControl(config_path, manager)`、关键字参数和默认 ResourceManager 路径保持兼容。
- `close_all()` 只负责连接关闭，不负责电源状态管理。

相关测试：

- `tests/test_instrument_power_control.py`
- `tests/test_instrument_control_injection.py`

第一、第二项不需要重复实施。后续修改如触及仪器控制或 GUI 配置边界，必须先运行上述回归测试。

## 4. 第三项：提取测量纯计算函数

### 4.1 调研结论与实际范围

第三项的目标是把公式和纯判定从硬件动作中提取出来，但不把同步测量类、增强工作线程和结果模型一次性合并。调研确认实际需要同时覆盖同步入口和 GUI 增强入口：

| 领域 | 同步实现 | GUI 增强实现 | 重构边界 |
| --- | --- | --- | --- |
| 线损最终计算 | `cable_loss_measurement.py:82-98` | `enhanced_workers.py:111-127` | 两处使用同一个纯函数 |
| 驱动输出功率补偿 | `driver_power_mapping.py:28-41` 仅做测量读数补偿 | 继承同步实现 | 保留补偿公式；插值属于主功放输入功率计算 |
| 主功放输出补偿 | `amplifier_measurement.py:59-65` | 继承同步实现 | 两个入口继续调用同一补偿函数 |
| 驱动映射插值 | `amplifier_measurement.py:68-84` | 继承同步实现 | 保留按输入功率排序及端点外推行为 |
| DUT 输入功率 | `amplifier_measurement.py:129-136` | `enhanced_workers.py:324-329` | 保留驱动模式/无驱动两条路径 |
| 增益和效率 | `amplifier_measurement.py:166-169` | `enhanced_workers.py:356-359` | 保留单位和 `dc_power <= 0` 时效率为 0 |
| 压缩点 | `amplifier_measurement.py:194-214` | `enhanced_workers.py:406-425` | 保留结果字段和小信号基准语义 |

建议优先在项目根目录新建 `measurement_calculations.py`，集中放置跨模块共享且无仪器依赖的函数。这样可避免把同一公式分别复制进三个测量类；新模块只接收数字、映射字典和列表，不导入 `InstrumentControl`、Qt 或项目配置文件。

当前实现需要先锁定的边界：

- 线损结果键使用字符串频率，计算结果包含 `cable1` 至 `cable4`、`total_path1`、`total_path2`。
- 驱动映射的输入键和值从 JSON 字符串转换为浮点，输入点按数值升序排序，`np.interp` 对范围外输入采用端点值。
- 主功放无驱动时使用 `sg_power - cable1`，有驱动时使用映射插值结果。
- 输出功率补偿为频谱仪读数加 `cable4 + attenuator + cable2`；驱动映射输出补偿为读数加 `attenuator + cable2`。
- 效率使用 `10 ** ((output_power_dbm - 30) / 10) / dc_power * 100`；DC 功耗不为正时返回 0。
- 压缩点以扫描前 3 个有效增益点的平均值为小信号增益，压缩量为 `small_signal_gain - gain`，取最接近目标值的点。
- 同步类当前对空扫描存在 `compression_gains.max()` 和 `argmin()` 风险；增强类在空扫描时也会访问空数组索引。该行为不能未经决定直接“修复”为新语义，必须先通过测试明确空扫描、保护在首点触发和有效点不足 3 个时的预期。

### 4.2 目标纯函数接口

第一阶段建议使用以下接口，函数名和返回字段在测试锁定后再实施：

```python
def calculate_cable_losses(path1_loss: float,
                           path2_loss: float,
                           attenuator_value: float) -> dict:
    """返回 cable1/cable2/cable3/cable4 及两条路径总损耗。"""

def compensate_amplifier_output_power(measured_power: float,
                                      frequency: float,
                                      loss_data: dict,
                                      attenuator_value: float) -> float:
    """补偿 DUT 输出端到频谱仪之间的 cable4、衰减器和 cable2。"""

def compensate_driver_output_power(measured_power: float,
                                   frequency: float,
                                   loss_data: dict,
                                   attenuator_value: float) -> float:
    """补偿驱动功放输出端到频谱仪之间的衰减器和 cable2。"""

def interpolate_driver_output_power(frequency: float,
                                    sg_power: float,
                                    driver_mapping: dict) -> float:
    """按 JSON 映射排序后执行线性插值，保持 np.interp 端点行为。"""

def calculate_dut_input_power(sg_power: float,
                              frequency: float,
                              loss_data: dict,
                              driver_mapping: dict | None) -> float:
    """统一有驱动和无驱动两条 DUT 输入功率路径。"""

def calculate_gain(output_power_dbm: float, input_power_dbm: float) -> float:
    return output_power_dbm - input_power_dbm

def calculate_efficiency(output_power_dbm: float, dc_power: float) -> float:
    """DC 功耗不为正时返回 0。"""

def calculate_compression_result(gains: list[float],
                                 input_powers: list[float],
                                 output_powers: list[float],
                                 efficiencies: list[float],
                                 sg_powers: list[float],
                                 compression_value: float,
                                 small_gain_points: int = 3) -> dict:
    """返回压缩点数据、small_signal_gain 和 compression_achieved。"""
```

函数接口不应携带 `self`、仪器对象、回调、日志或停止标志。`calculate_compression_result()` 的空列表和不足 3 点行为必须在阶段 0 决策并写入测试，不应由实现者自行改变。

### 4.3 分阶段具体执行计划

#### 阶段 0：冻结公式和边界基线

1. 备份 `cable_loss_results.json`、最近一次 `amplifier_measurement_*.json` 和 `driver_power_mapping_*.json`；不修改现场文件。
2. 从现有代码固定公式快照：线损分摊、两类输出功率补偿、驱动插值、DUT 输入功率、增益、效率和压缩点。
3. 固定最小脱敏样例：一个频率的线损字典、一个有序/乱序驱动映射、一个无驱动扫描点列表和一个主功放扫描结果列表。
4. 明确并测试以下边界预期：空输入、少于 3 个有效增益点、首个点触发最大 DUT 输入保护、DC 功耗为 0/负数、目标压缩点未达到、映射超出输入范围、频率键不存在。
5. 建立 `tests/test_measurement_calculations.py`，先只测试预期行为，不修改生产测量流程。

阶段 0 的通过条件是公式和边界预期已形成可审查的测试；对空扫描等当前会抛异常的场景，必须明确选择“保留异常”还是“定义空结果”，不能静默改变行为。

#### 阶段 1：提取线损和功率补偿函数

1. 新建 `measurement_calculations.py`，实现 `calculate_cable_losses()`。
2. 将同步 `CableLossMeasurement.measure_all_frequencies()` 和增强 `_measure_step2()` 的四个线损表达式改为调用该函数。
3. 实现两个独立的输出功率补偿函数，分别接入 `AmplifierMeasurement.calculate_actual_power()` 和 `DriverPowerMapping.calculate_actual_power()`；保留原方法作为兼容包装入口。
4. 测试线损结果字段完整、频率无关计算不改变键、浮点结果在允许误差内一致。
5. 测试同步类和增强类都调用相同纯函数，避免只修复 CLI 入口而遗漏 GUI 入口。

#### 阶段 2：提取驱动插值和 DUT 输入功率

1. 将 `get_driver_output_power()` 的 JSON 键值转换、排序和 `np.interp` 提取为 `interpolate_driver_output_power()`。
2. 保留乱序映射排序、重复输入点当前 NumPy 行为和范围外端点行为；如要改变，另行立项。
3. 将有驱动/无驱动分支提取为 `calculate_dut_input_power()`，让同步 `perform_power_sweep()` 和增强 `perform_power_sweep()` 共用。
4. 增加驱动模式存在、驱动模式为空、无驱动、频率缺失和范围外输入测试。
5. 验证最大 DUT 输入功率判断仍发生在 `set_power()` 之前，保护触发时不新增测量点、不改变停止位置。

#### 阶段 3：提取增益、效率和压缩点

1. 提取 `calculate_gain()` 和 `calculate_efficiency()`，保留输出单位和 DC 功耗非正时效率为 0。
2. 提取 `calculate_compression_result()`，输入已测量列表，输出现有 `compression_point`、`small_signal_gain`、`compression_achieved` 所需数据。
3. 同步和增强两套 `perform_power_sweep()` 都调用同一压缩结果函数；实时消息和 `data_callback` 仍留在工作线程中。
4. 对正常达到压缩点、未达到压缩点、扫描提前停止和有效点不足 3 个分别建立固定输入测试。
5. 空扫描的处理以阶段 0 决策为准：若保留当前异常，测试异常类型/位置；若定义空结果，必须同时更新同步和增强入口，并记录结果兼容影响。

#### 阶段 4：接入测量循环并验证结果兼容

1. 保留 `perform_power_sweep()`、`measure_power_mapping()`、`measure_all_frequencies()` 等公有方法和硬件动作顺序。
2. 只替换公式调用，不移动 `set_power()`、等待、RF 开关、频谱仪读取、电源读取、停止检查和结果保存位置。
3. 运行同步路径和增强路径的仿真测量回归，比较 `sweep_data`、压缩点字段、驱动映射字段和线损 JSON 字段。
4. 对最近一次脱敏结果做字段级比较；允许极小浮点差异，不允许字段缺失、单位变化、频率键变化或扫描点数量变化。
5. 完成后再考虑删除类内重复计算代码；公有兼容包装和纯函数保留，避免影响外部调用。

### 4.4 文件级修改清单

| 文件 | 计划修改 | 明确不修改 |
| --- | --- | --- |
| `measurement_calculations.py` | 新增纯函数和类型/文档说明 | 不导入 VISA、Qt、配置文件或写结果文件 |
| `cable_loss_measurement.py` | 调用线损纯函数，保留测量交互和保存格式 | 不改路径更换流程、RF 操作和结果 JSON |
| `driver_power_mapping.py` | 调用驱动输出补偿纯函数 | 不改扫描范围、等待、RF 和映射 JSON 格式 |
| `amplifier_measurement.py` | 调用补偿、插值、输入功率、指标和压缩纯函数 | 不改硬件动作、保护检查、结果结构和清理路径 |
| `enhanced_workers.py` | 让增强线损、映射和主功放复用同一纯函数 | 不改 Qt 信号、停止逻辑、实时数据结构和线程流程 |
| `tests/test_measurement_calculations.py` | 新增纯计算和边界测试 | 不依赖真实 VISA 或 Qt |
| 现有测试文件 | 必要时补充同步/增强接入回归 | 不修改测试去迎合现场 `config.json` |

### 4.5 验收要求

纯计算层至少覆盖：

- 线损正常值、零值、负值和浮点结果。
- 两类输出功率补偿及衰减器值变化。
- 驱动映射乱序键、范围内插值、范围外端点、频率缺失。
- 有驱动和无驱动的 DUT 输入功率。
- 增益、效率及 DC 功耗为 0/负数。
- 压缩点达到、未达到、空列表、提前停止和不足 3 点。

接入层至少验证：

- 同步测量类与增强工作线程结果一致。
- 保护判断仍早于设置功率和硬件测量。
- RF 开关、等待、电源读取和停止流程未改变。
- 结果 JSON 的字段、键、单位和扫描点数量兼容。

执行顺序：纯函数测试通过后，再执行同步/增强仿真回归；完整自动化通过后，才允许低功率、单频点或极短扫描真实设备验收。

## 5. 通用验证与现场约束

每次后续重构完成后，在 `Auto_test` 环境执行：

```powershell
python -m compileall -q -f -x '__pycache__|test_results' .
python -m unittest discover -s tests -v
```

涉及 GUI 或仪器控制时，还应执行：

- GUI 导入冒烟，确认不自动建立 VISA 连接。
- 仿真仪器连接、初始化、上电、掉电和清理测试。
- 结果文件读取测试。
- 真实设备验收前的配置、通道接线、RF 默认状态和紧急断电条件检查。

当前已知基线差异：完整测试中存在 2 项与工作区 `config.json` 驱动模式状态不一致的既有失败。不得修改现场配置或修改测试迎合现场状态；应在后续测试记录中单独说明。

真实设备验收必须使用低功率、单频点或极短扫描范围，并记录实际电源状态、SCPI 日志、异常信息、人工观察结果和回滚结论。当前前三项的代码重构及自动化/仿真验证已完成，真实设备验收尚未执行。

阶段4自动化接入验证已完成：`Auto_test` 编译检查通过；纯计算测试和同步/增强测量循环回归共 52 项通过。全量测试中仍有 2 项与工作区 `config.json` 驱动模式状态不一致的既有失败，未修改现场配置或测试预期。

## 6. 工作区注意事项

以下文件可能包含现场或测试产物修改，不得在重构过程中回退或自动覆盖：

- `config.json`
- `cable_loss_results.json`
- `amplifier_measurement_20260820_205415.json`

后续提交只应包含与当前重构直接相关的源码、测试和文档变更。完成第三项后，再同步更新 `重构基线.md` 和 `重构执行.md`，记录实际验证命令、真实设备验收状态、行为差异和遗留风险。
