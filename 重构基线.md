# PA 自动测试系统重构基线

> 文档性质：长期架构基线、约束和决策记录。
>
> 本文回答“系统应该是什么样”，不替代 `重构执行.md` 中的具体任务清单。
>
> 建立日期：2026-08-19  |  基线版本：0.2  |  最后更新：2026-09-25

## 1. 重构结论

本项目确实需要进行架构重构，但不适合直接推倒重写。当前代码已经完成一部分低风险治理，下一阶段应采用“接口先行、仿真验证、渐进替换、旧入口兼容”的方式推进。

当前最大的结构问题不是目录平铺本身，而是职责边界没有真正落地：

- `enhanced_main_gui.py` 同时承担界面、配置、线程、测量启动、结果读写和可视化协调。
- `enhanced_workers.py` 通过继承同步测量类复制并改写核心流程，存在行为漂移。
- 测量模块直接创建 `InstrumentControl`，并依赖旧配置字典和文件路径。
- `InstrumentControl` 同时处理 VISA 连接、SCPI、供电安全顺序和运行配置。
- 结果和中间数据仍以隐式字典键为主要接口。
- 新的配置模型、结果存储和生命周期模块已经存在，但还没有成为完整生产主链路。

因此，重构目标是建立清晰的依赖方向和可测试的运行边界，而不是单纯重新排列文件。

## 2. 当前系统画像

### 2.1 生产链路

1. `start_gui.bat`、`launcher.py` 或 `vscode_launcher.py` 启动应用。
2. `launcher.py` 检查环境和依赖后导入 `enhanced_main_gui.py`。
3. GUI 创建配置、仪器控制、测量类、工作线程、可视化和 AI 对象。
4. Qt 工作线程调用 `enhanced_workers.py` 中继承同步测量类的实现。
5. 测量类直接读取配置、创建仪器、执行扫描、保存结果并进行清理。
6. 结果主要保存为 JSON，同时保留旧路径兼容副本。

### 2.2 当前模块职责

| 区域 | 当前主要文件 | 当前状态 |
| --- | --- | --- |
| 启动 | `launcher.py`、`vscode_launcher.py`、`start_gui.bat` | 入口重复，`launcher.py` 是目标生产入口 |
| GUI | `enhanced_main_gui.py` | 约 3600 行，职责严重混合 |
| 工作线程 | `enhanced_workers.py` | 依赖 Qt，并复制测量业务逻辑 |
| 仪器 | `instrument_control.py` | VISA、SCPI、电源安全、配置读取混合 |
| 测量 | 三个 `*_measurement.py` | 业务编排、硬件操作、结果保存混合 |
| 计算 | `measurement_calculations.py` | 已提取部分纯计算，方向正确 |
| 配置 | `config_models.py`、`config_validation.py` | 已有模型和校验，但旧字典仍是主链路输入 |
| 结果 | `result_storage.py`、`result_reading.py` | 已有快照、原子写入和兼容读取基础 |
| 分析 | `data_visualization.py` | 直接依赖结果结构，绘图和报告耦合 |
| AI | `llm.py`、`web_search_tools.py` | 可选能力，必须与主测量链路隔离 |
| 遗留子系统 | `DPD_auto_test/` | 暂不纳入本次主系统重构 |

### 2.3 已完成的基础工作

截至 2026-08-21，已完成或部分完成：

- 项目路径、配置 JSON 读取和基础日志统一。
- 配置校验、配置模型和旧配置转换基础。
- 线损、驱动映射、功放测试的部分纯计算函数提取。
- 结果 JSON 原子写入、运行 ID、运行目录和旧路径兼容副本。
- 运行快照在连接仪器前创建的基础流程。
- 测量生命周期中的 RF、供电和连接清理包装。
- 等待函数注入、显式电源角色和离线测试。
- 启动器、配置、路径、纯计算和部分测量循环自动化测试。

这些工作降低了后续风险，但不代表新架构已经完成。同步测量类、增强测量类和巨型 GUI 仍是生产主路径。

## 3. 目标架构

目标采用简化的六边形架构。依赖只能由外层指向内层，领域和测量服务不能反向依赖 Qt、PyVISA 或具体文件格式。

```text
Qt GUI / CLI
     |
     v
Application services
     |
     v
Domain: plans, run context, results, calculations, safety rules
     |
     +--> Instrument ports --------> VISA / simulation adapters
     |
     +--> Persistence ports -------> JSON / files / future SQLite index
     |
     +--> Analysis ports ----------> plots / reports / exports
```

推荐目标目录语义如下。目录迁移必须在接口稳定且有测试后执行，不能把目录搬动当作重构完成标志。

```text
pa_auto_test/
├── app/
│   ├── services/          # 运行用例编排
│   ├── events.py          # 测量事件
│   └── cancellation.py    # 普通停止和紧急停止协议
├── domain/
│   ├── config/            # TestPlan、RunContext 和校验规则
│   ├── measurements/      # 测量结果、扫描点和计算规则
│   ├── safety/            # 安全策略和生命周期状态
│   └── errors.py
├── instrument/
│   ├── ports.py           # 信号源、频谱仪、电源接口
│   ├── visa/              # PyVISA/SCPI 实现
│   ├── simulation/        # 仿真和命令录制实现
│   └── safety/            # 电源上电/掉电策略
├── persistence/
│   ├── config_repository.py
│   ├── result_repository.py
│   ├── legacy_readers.py
│   └── json_storage.py
├── analysis/              # 结果读取、绘图、报告和导出
├── presentation/qt/       # 窗口、页面、对话框和 Qt Worker
├── assistant/             # LLM、联网搜索和聊天历史
├── legacy/                # 过渡包装和旧格式适配器
├── tests/
└── launcher.py
```

## 4. 核心模型和边界

### 4.1 测试方案、运行上下文和结果快照

系统必须区分以下对象：

```text
TestPlan
    稳定测试方法和参数：频率、功率扫描、压缩点、补偿和保护值。

RunContext
    本次现场资源：仪器地址、电源设备、通道映射、操作者和接线确认。

MeasurementSession
    run_id、状态、开始/结束时间、停止原因、异常和事件流。

MeasurementResult
    原始读数、补偿读数、计算指标、方法版本和结果状态。
```

正式运行链路必须是：

```text
加载 TestPlan
 -> 填写或选择 RunContext
 -> 离线校验
 -> 创建运行快照
 -> 创建 InstrumentSession
 -> 测量
 -> 保存结果和报告
```

快照必须在连接仪器、上电或打开 RF 之前成功写入。

### 4.2 仪器端口

测量服务只依赖端口，不依赖 PyVISA：

- `SignalGeneratorPort`：频率、功率、RF 开关。
- `SpectrumAnalyzerPort`：中心频率、带宽、功率测量。
- `PowerSupplyPort`：通道电压、电流、保护、输出和读数。
- `InstrumentSession`：初始化、关闭和资源状态。

当前 SCPI 命令保留在 VISA 适配器中，第一阶段不改变命令内容。

### 4.3 测量服务和事件

线损、驱动映射和主功放测试各自是一个测量服务。同步 CLI 和 Qt Worker 必须调用同一服务。

服务通过通用事件报告：

```text
progress   进度
message    用户可见状态
data       实时扫描点或曲线数据
checkpoint  等待现场操作
completed  正常完成
stopped    用户停止
failed     失败和异常
```

服务不直接发送 Qt Signal、不读取 GUI 控件、不打印控制台专用文本。

### 4.4 安全生命周期

统一状态为：

```text
created -> validated -> connected -> prepared -> powered -> measuring
                                                    |
                         stopped / failed ----------+
                                                    v
                                      RF off -> power off -> close
```

清理必须幂等，并尽力执行所有后续清理动作。硬件未完全连接或某一步失败时，仍应尝试关闭其他已知资源。

普通停止允许完成当前安全边界；紧急停止优先关闭 RF，然后进入电源和连接清理。

## 5. 不可破坏的业务契约

在真实设备验收之前，不得以“架构重构”为理由改变以下行为：

1. 线损两步路径、衰减器补偿公式和结果字段语义。
2. 驱动映射的输入/输出关系、线损补偿位置和按频率保存方式。
3. 主功放有驱动/无驱动两种模式、扫描范围、压缩点计算和效率计算。
4. DUT 最大输入功率保护触发后不得继续设置更高激励。
5. RF 默认关闭；正常完成、停止、异常和窗口关闭都必须清理。
6. 电源遵守先栅后漏上电、先漏后栅掉电的顺序。
7. 旧结果在迁移期间可读取，旧启动入口在过渡期可回滚。
8. 每次运行保存实际方案、资源映射、软件版本、状态和异常信息。

## 6. 已确定和待确定的架构决策

### 6.1 已确定

- `launcher.py` 作为目标生产入口，其他入口作为兼容包装。
- 保留 CLI 测量能力，GUI 和 CLI 共用应用服务。
- 测试方案与单次现场资源映射分离。
- 第一版数据继续以 JSON 保存完整快照和原始结果。
- `DPD_auto_test/` 不进入主系统重构。
- 仪器端口从第一版开始抽象，但第一版只实现当前 PyVISA/SCPI 设备。
- 新实现采用功能开关或兼容包装逐步切换。

### 6.2 需要项目负责人确认

1. 第一版是否允许多台电源同时参与一次运行；建议数据模型支持多台，首个业务实现可先限制一台。
2. 是否增加 SQLite 作为运行索引；建议 JSON 保留原始数据，SQLite 只保存历史运行摘要和路径。
3. 普通停止和紧急停止的现场语义及验收标准。
4. 旧 `config.json` 和旧结果兼容期；建议旧结果读取长期保留，旧配置至少保留一个生产周期。
5. 当前仪器品牌是否会在近期更换；这决定适配器实现的优先级，不影响端口抽象。
6. 是否需要连续任务、队列运行、暂停恢复；第一版可只支持单次运行，但内部保留 `MeasurementSession`。

## 7. 发布、验收和回滚原则

- 每次只改变一个可观察行为。
- 公式、SCPI、安全顺序和文件格式不在同一批变更中同时调整。
- 每个阶段必须有自动化验证和明确的停止条件。
- 涉及硬件的步骤必须增加真实设备验收，仿真不能替代真实验收。
- 新实现首次上线必须可切回旧实现。
- 出现测量值异常、DUT 保护失效、RF/电源未关闭、结果不可读或 GUI 无法启动时，立即关闭新实现并回滚。
- 不提交真实 IP、密钥、聊天历史、现场结果和未经脱敏的设备信息。

## 8. 里程碑记录

| 日期 | 阶段 | 内容 | 验证 |
| --- | --- | --- | --- |
| 2026-08-19 | 基线建立 | 完成目录、入口、依赖、主链路和风险调研 | 编译检查；待真实设备验收 |
| 2026-08-19 | 阶段 0 | 增加配置校验、启动器退出码、路径和 GUI 导入测试 | 31 项自动化测试；未执行测量 |
| 2026-08-21 | 低风险重构 | 配置读写、日志、纯计算、结果存储、生命周期、等待注入和电源角色治理 | 242 项离线测试；未完成真实设备验收 |
| 2026-09-25 | 架构基线更新 | 明确以端口、应用服务、领域模型、仿真仪器和事件流为下一阶段重点 | 尚未开始阶段 1 |
