# PA 自动测试系统重构基线

> 文档性质：长期基线与架构治理说明。本文记录目标、现状、约束、原则和阶段路线，不替代具体执行清单。
>
> 建立日期：2026-08-19  |  基线版本：0.1  |  适用分支：当前默认分支

## 1. 重构目标

### 1.1 业务目标

- 保持每日一次的生产使用连续性，任何阶段都能启动现有 GUI 并完成已有测试流程。
- 保留并可追溯现有测量结果、线损结果、驱动映射结果、测试报告和聊天历史；每次运行保存当时实际使用的测试参数与现场资源映射。
- 将硬件安全行为（射频开关、电源上电/掉电、DUT 输入功率保护、紧急停止）作为不可回归的业务契约。
- 让后续改动可以在无真实仪器的环境中完成大部分验证，在真实设备上只做小范围验收。

### 1.2 技术目标

- 明确分离界面层、应用编排层、测量领域逻辑、仪器适配层和数据存储层。
- 消除同步脚本与 GUI 增强线程之间的核心逻辑重复，建立一个可复用的测量服务。
- 用有版本的测试方案、单次运行配置和结果模型替代散落的字典键约定，并保留旧 JSON 的读取兼容期。
- 统一入口、路径、日志、异常处理、资源清理和结果导出策略。
- 建立最小但可靠的测试体系：纯计算单元测试、仿真仪器集成测试、启动冒烟测试和真实设备验收记录。

## 2. 当前系统画像

### 2.1 当前生产链路

1. `start_gui.bat`、`launcher.py` 或 `vscode_launcher.py` 启动应用。
2. `launcher.py` 检查 Python 和依赖后直接导入 `enhanced_main_gui.py`。
3. GUI 创建配置、仪器控制、线损测量、驱动功率映射、主功放测量、可视化和 AI 助手对象。
4. 测量工作线程调用 `enhanced_workers.py` 中继承自同步测量类的实现，并通过回调更新 GUI。
5. 结果以 JSON 保存到项目根目录，报告输出到 `test_results/`。

### 2.2 代码与资产分布

| 区域 | 当前职责 | 主要文件/资产 | 基线判断 |
| --- | --- | --- | --- |
| 启动 | 环境检查、路径切换、GUI 选择 | `launcher.py`、`vscode_launcher.py`、`start_gui.bat` | 入口重复，生产入口需收敛 |
| GUI | 所有页面、对话框、线程调用、配置和文件操作 | `enhanced_main_gui.py`（约 3548 行） | 最大耦合点，暂不直接整体重写 |
| 硬件 | VISA 连接、SCPI、上/掉电序列、读数 | `instrument_control.py` | 安全关键，优先建立接口和仿真替身 |
| 测量 | 线损、驱动映射、主功放扫描 | `cable_loss_measurement.py`、`driver_power_mapping.py`、`amplifier_measurement.py` | 计算和硬件操作混合，存在同步/增强版重复 |
| 工作线程 | 进度、停止、实时数据回调 | `enhanced_workers.py` | 继承业务类并覆写方法，边界不稳定 |
| 分析 | 曲线、CSV、HTML/PDF 图表 | `data_visualization.py` | 直接依赖结果字典，报告生成与绘图耦合 |
| AI | Ollama 对话、历史、联网搜索 | `llm.py`、`web_search_tools.py` | 非主测试链路，应隔离为可选能力 |
| 遗留子系统 | DPD/ILA/ILC Notebook 和函数 | `DPD_auto_test/` | 独立治理，暂不混入主链路重构 |
| 工具 | 格式转换和连接图 | `format_convert/`、`connection_diagrams.py` | 可后置整理 |
| 运行产物 | 历史 JSON、图片、PDF、CSV、HTML、MDF | 根目录、`test_results/`、`power_data.mdf` | 需要明确归档、版本和忽略策略 |

### 2.3 已确认事实

- 当前 Git 最近提交已处理项目路径稳定性：`project_paths.py` 集中定义路径，`launcher.py` 切换到项目根目录。
- 当前默认输入是 `config.json`，其中混合了测试方案参数、仪器地址和现场电源映射；由于电源设备可能更换、IP 地址可能变化，不能把其中的设备地址和通道连接关系视为固定生产配置。
- 已验证完整 GUI 依赖和模块导入的 Conda 环境为 `Auto_test`（Python 3.11.15，路径 `C:\My_Document\Anaconda\envs\Auto_test`）；历史文档中的 `VISA_demo` 在当前机器不存在。
- 当前文件启用了 `PS4` 和驱动模式，但驱动功放供电分配为空；这应被视为“默认运行映射不完整”，由每次测试开始前的现场资源配置和接线确认处理，而不是假定存在永久固定接线。
- 项目可通过 `python -m compileall -q -f -x '__pycache__|test_results' .` 编译检查。
- 已建立 31 项基础自动化测试，覆盖配置校验、路径、启动器与 GUI 模块导入；仍缺少依赖锁定和 CI 配置。
- README 中原先引用不存在的 `main_gui.py` 已在第一轮修正，后续仍需持续检查文档与入口一致性。
- `instrument_control.py`、三个测量模块和 GUI 大量使用字符串键、相对/默认文件和裸异常处理。
- `amplifier_measurement.py` 与 `enhanced_workers.py` 的功率扫描逻辑重复但等待时间、回调和错误处理不同，存在行为漂移风险。
- AI 服务地址、搜索 API 配置和历史文件属于可选能力；AI 不得阻塞或改变主测量链路。
- 配置校验拒绝非有限数值、非法 dB 值和无效电源引用；仅对本次运行选定的电源检查工作值与保护值关系，避免把未使用设备的历史参数误报为运行风险。

## 3. 不可破坏的业务契约

以下行为在没有真实设备验收记录前不得通过“重构”改变：

1. 线损两步测量的路径定义、衰减器补偿公式和结果字段语义。
2. 驱动映射的输入功率到输出功率关系、线损补偿位置和按频率保存方式。
3. 主功放测试的 SG 功率扫描、驱动模式/无驱动模式、DUT 最大输入功率停止条件、压缩点计算、DC 功耗和效率计算。
4. 射频输出默认关闭；异常、停止、窗口关闭和正常完成都必须执行资源清理。
5. 电源“先栅后漏”上电、“先漏后栅”掉电顺序，以及当前配置的保护值。
6. 旧结果 JSON 在迁移期间可读取；新格式必须可标识版本、测量时间和本次运行实际使用的资源映射。
7. 每次生产改动都保留可回滚的旧入口或旧实现，并能在同一天恢复使用。

## 4. 重构原则

### 4.1 渐进式替换

- 以“增加边界、包裹旧实现、双读/单写、逐步切换”为主要手段。
- 一次只改变一个可观察行为；不在同一变更中同时调整测量公式、文件格式和 GUI 流程。
- 新实现先以影子模式、仿真模式或旧实现对照运行，再成为默认实现。
- 每个阶段都要有明确的停止条件、验收证据和回滚动作。

### 4.2 安全优先级

1. DUT/仪器安全和资源清理。
2. 测量数据正确性与结果可追溯。
3. 每日生产可用性。
4. 可维护性、测试性和结构美观。

### 4.3 依赖方向

目标依赖方向如下，低层不得反向依赖 GUI：

```text
启动/CLI -> 应用编排 -> 测量领域服务 -> 仪器端口 -> VISA/真实仪器
                         -> 配置/结果端口 -> JSON/文件系统
GUI --------------------> 应用编排（通过信号/事件接收进度）
分析/报告 ----------------> 结果模型/读取端口
AI（可选）---------------> 独立应用服务，不进入测量核心
```

## 5. 配置模型与目标架构

### 5.1 配置模型

系统需要区分长期稳定的测试方案和每次测试变化的现场运行配置，不再要求固定完整的仪器接线配置：

- **测试方案**：频率范围、功率扫描、压缩点规则、DUT 安全限制、补偿参数和测量方法版本。这部分可以版本化并作为模板保存。
- **单次运行配置**：本次实际使用的信号源、频谱仪、电源 VISA 地址、电源通道映射、驱动模式、现场接线说明和操作者确认。这部分在每次测试前确认，允许随设备更换和 IP 变化而改变。
- **结果快照**：将测试方案、单次运行配置、软件版本、开始时间、状态和结果一起保存，使结果可追溯，而不要求下一次运行复用相同地址或通道。

运行前仍必须校验当前映射：设备和通道存在、通道没有重复分配、驱动模式和 DUT 通道已明确、电气保护参数完整且操作者已确认现场接线。未使用设备的历史配置不应阻断本次运行。

推荐最终形成以下目录语义（名称可在执行阶段微调）：

```text
pa_auto_test/
├── app/                 # 用例编排、运行状态、命令入口
├── domain/              # 配置、测量参数、结果、计算规则和安全策略
├── instrument/          # 仪器端口、PyVISA 适配器、仿真适配器
├── measurement/         # 线损、驱动映射、主功放测量服务
├── persistence/         # 配置、结果、报告文件读写和版本迁移
├── presentation/        # Qt 窗口、页面、线程/任务适配
├── analysis/             # 可视化和报告
├── assistant/            # LLM 和联网搜索，可选
├── legacy/               # 过渡期兼容包装和 DPD 遗留入口
└── tests/                # 单元、仿真集成、启动冒烟和契约测试
```

目标架构不是立即搬目录；只有当模块有测试和稳定接口后才迁移文件。

## 6. 分阶段路线

| 阶段 | 主题 | 结果 | 业务风险 |
| --- | --- | --- | --- |
| 0 | 基线冻结与可观测性 | 入口、配置、结果、日志和验收样例明确 | 低 |
| 1 | 配置/路径/依赖治理 | 配置校验、统一运行目录、可复现环境 | 低至中 |
| 2 | 硬件端口与仿真仪器 | 测量服务可脱离真实 VISA 测试 | 高，需严格验收 |
| 3 | 测量领域服务抽离 | 去除同步/增强核心逻辑重复，统一安全清理 | 高 |
| 4 | 结果模型与持久化迁移 | 版本化 JSON、旧格式兼容、报告稳定 | 中 |
| 5 | GUI 分层与入口收敛 | GUI 只负责展示和交互，保留旧启动兼容 | 中 |
| 6 | 分析/AI/工具/DPD 隔离 | 可选能力独立，遗留系统单独演进 | 低至中 |
| 7 | 删除过渡层与长期维护 | 清理旧实现，形成发布和回滚机制 | 中 |

阶段不是按日历强制推进；每个阶段完成后至少经过一次真实日常使用周期再进入下一阶段。日常使用周期允许更换电源、改变 IP 和现场通道映射，但必须保存当次运行快照。

## 7. 发布与回滚策略

- 每次变更保持小提交，提交信息标明阶段和风险等级。
- 变更前保存当前测试方案、入口校验结果和最近一次运行快照；不把真实密钥提交到 Git，也不要求将变化的现场设备地址固化为永久配置。
- 变更后至少执行：编译检查、仿真测试、启动冒烟、结果文件读取检查；涉及硬件时增加受控真实设备验收。
- 首次上线新实现时保留功能开关，默认可回到旧实现。
- 真实设备验收必须记录当次设备连接、通道映射、现场接线确认、频率、功率范围、结果差异、异常清理和操作者确认。
- 如出现测量值异常、功率保护未触发、RF/电源未关闭、结果无法读取或 GUI 无法启动，立即关闭新实现并恢复旧入口；问题进入执行文档遗留问题区。

## 8. 基线更新规则

- 本文更新“目标、现状、阶段、架构决策、已完成里程碑和新风险”。
- `重构执行.md` 只保留未完成事项；完成事项移入本文的里程碑或变更记录。
- 每次重构结束时同时更新两份文档，并记录日期、提交、验证命令和是否经过真实设备验收。
- 若实际代码与本文冲突，以代码和生产验收事实为准，同时在下一次变更中修正文档。

## 9. 里程碑记录

| 日期 | 阶段/提交 | 已完成内容 | 验证与备注 |
| --- | --- | --- | --- |
| 2026-08-19 | 基线建立 | 完成目录、入口、依赖、核心测量链路和风险调研；确认编译检查通过 | 尚未开始真实设备回归 |
| 2026-08-19 | 阶段 0（进行中） | 增加只读配置校验、`--validate-config`、启动器退出码、路径/配置/启动器测试；修正启动文档中的失效入口 | 编译通过；18 项测试通过；跨工作目录配置校验通过；待完成具备 GUI 依赖环境的无 VISA 启动和真实设备验收 |
| 2026-08-19 | 阶段 0 修复 | 配置校验拒绝 NaN/无穷数和非法 dB 值；补充电源保护风险警告；启动器识别完整 GUI 顶层依赖、拒绝未知/互斥参数并保留旧函数调用形态；新增 GUI 导入冒烟测试 | 源码解析通过；30 项测试通过、1 项因缺少完整 GUI 依赖跳过；真实 GUI/仪器验收未执行 |
| 2026-08-19 | 阶段 0 环境迁移 | 统一日常启动环境为 `Auto_test`，批处理入口激活该环境并转发参数；修正历史环境和项目路径说明 | `start_gui.bat --check` 与 `--validate-config` 通过；GUI 进程存活 8 秒后由测试主动终止；正常窗口关闭和硬件状态未验收 |
| 2026-08-19 | 阶段 0 验证补充 | 在 `Auto_test` 中完成依赖检查、配置校验、GUI 模块导入和 GUI 进程短时启动验证 | 31 项自动化测试通过；GUI 未执行测量、仪器连接、RF 或电源操作；待人工正常关闭窗口并确认硬件状态 |
| 2026-08-19 | 重构决策调整 | 明确采用“测试方案模板 + 单次运行资源映射 + 结果快照”模型；现场电源设备、IP 和通道接线不作为永久固定配置 | 尚未修改代码；后续阶段 0、1、2 按运行时映射和每次快照执行 |
| 2026-08-21 | 前三项低风险重构 | 完成 GUI 配置读写拆分、仪器初始化/电源安全流程拆分、测量纯计算函数提取；同步和增强测量入口接入共享纯函数 | `Auto_test` 中编译检查通过；纯计算与测量循环回归 52 项通过；全量测试 187 项中 2 项为现有现场 `config.json` 驱动模式状态差异；真实设备验收未执行 |
| 2026-08-21 | 第二批低风险重构 | 1) 配置校验与旧配置转换测试改用固定夹具 `tests/fixtures/`，不再读取现场 `config.json`；2) 新增 `app_logging.py` 最小日志基础设施并在 launcher/GUI 入口挂载，测量与仪器控制关键事件点（初始化、RF 开关、上下电序列、DUT 保护触发、测量开始/停止/失败、清理、结果保存）追加日志，控制流与界面行为不变；3) 时间戳测量 JSON、`power_data.mdf`、`logs/` 出库并加入 `.gitignore`（本地文件保留） | 编译检查通过；全量测试 195 项通过（修复此前 2 项失败）；`launcher.py --validate-config` 退出码 0 且日志文件生成于 `logs/`；真实设备验收未执行 |
| 2026-08-21 | 第三批低风险重构 | 旧配置转换增加缺失配置段边界报告；新增 `result_storage.py`，统一兼容 JSON 读取、带元数据写入、原子替换和运行目录创建；新增 `measurement_lifecycle.py`，同步/增强测量入口统一 RF、电源、连接清理；保留旧结果字段、旧文件名、SCPI 和电源顺序 | `Auto_test` 环境受影响测试 57 项通过；全量编译和测试待完成；真实设备验收未执行 |
| 2026-08-21 | 第三批审查修复 | `run_id` 限制为安全单层目录名并提供程序生成；线损、驱动映射和主功放结果均先归档到 `test_results/<run_id>/`，再保留旧路径兼容副本；运行目录冲突时不会先覆盖旧结果 | `Auto_test` 环境受影响测试 57 项通过；全量编译和测试待完成；真实设备验收未执行 |
