# PA 自动测试系统重构基线

> 文档性质：长期架构基线、约束和决策记录。
>
> 本文回答“系统当前是什么样、目标应是什么样”；具体执行状态和剩余任务见 `重构执行.md`。
>
> 建立日期：2026-08-19  |  基线版本：0.5  |  最后更新：2026-09-26

## 1. 当前结论

本项目采用渐进式重构，不推倒重写。离线重构能力已经完成并接入兼容入口；真实设备验收、脱敏生产基线、稳定生产周期和过渡层清理尚未完成。因此当前仍采用“新服务验证、旧入口兼容、现场验收后清理”的策略。

截至 2026-09-26，指定 Python 环境下自动化测试为 `294 tests, OK`；`launcher.py --check` 和 `launcher.py --validate-config` 均通过。端口协议、仿真仪器、安全清理、测量服务、运行快照、结果模型、历史读取、报告和可选能力边界均已有离线测试。以上只证明离线行为，不等同于真实设备生产验收。

当前仍未完成的结构迁移：

- `enhanced_main_gui.py` 仍是约 3600 行的兼容外壳，页面注册边界已建立，但页面尚未物理拆分。
- Qt Worker 在服务创建前执行 `PreparedRun` 预检和快照，但随后仍创建 `Enhanced*` 兼容适配器。
- 兼容适配器调用统一测量服务，但生产仪器仍由 `InstrumentControl` 提供，执行输入仍含旧配置字典。
- 新配置、运行上下文和结果模型已用于预检、快照和结果边界，尚未完全成为生产执行主契约。
- 仓库中的 `baseline/collected/...` 只是历史文件结构样例，不能视为脱敏生产基线；发布清单的真实设备验收状态仍为 `pending`。

## 2. 当前系统画像

### 2.1 当前生产链路

1. `start_gui.bat`、`launcher.py` 或 `vscode_launcher.py` 启动应用；`launcher.py` 是目标生产入口。
2. GUI Worker 通过 `app.gui_runtime.prepare_configuration()` 加载并校验配置，生成 `run_id`、运行目录和快照。
3. 快照成功后，Worker 创建 `EnhancedCableLossMeasurement`、`EnhancedDriverPowerMapping` 或 `EnhancedAmplifierMeasurement`。
4. 兼容适配器调用 `measurement_services.py` 中的统一服务；仪器操作仍通过 `InstrumentControl`。
5. 测量服务执行扫描、发布事件并通过生命周期包装完成 RF、供电和连接清理。
6. 结果写入版本化运行目录，同时保留旧路径兼容副本；历史 JSON 可通过只读适配器读取。

### 2.2 当前模块职责

| 区域 | 当前主要文件 | 当前状态 |
| --- | --- | --- |
| 启动 | `launcher.py`、`vscode_launcher.py`、`start_gui.bat` | `launcher.py` 为目标入口，其余为兼容转发 |
| GUI | `enhanced_main_gui.py`、`presentation/qt/pages.py` | 页面注册已集中，窗口职责仍混合 |
| Worker | `presentation/qt/workers.py`、`enhanced_workers.py` | Qt Worker 负责线程和信号，兼容适配器仍存在 |
| 仪器 | `instrument/ports.py`、`instrument/simulation.py`、`instrument_control.py` | 端口和仿真已建立，真实 VISA 适配仍待验收 |
| 测量 | `measurement_services.py`、三个旧测量入口 | 核心编排已统一，旧入口仍保留 |
| 计算 | `measurement_calculations.py` | 纯计算函数已集中并有回归测试 |
| 配置 | `config_models.py`、`persistence/config_repository.py`、`config_validation.py` | 新模型、旧配置转换和预检已存在 |
| 结果 | `result_storage.py`、`result_reading.py`、`domain/models.py` | 快照、原子写入、版本化模型和兼容读取已存在 |
| 分析 | `data_visualization.py`、`analysis/reporting.py` | 新报告边界已建立，旧可视化入口仍保留 |
| 可选能力 | `assistant/`、`format_convert/`、`DPD_auto_test/` | 与主测量入口隔离 |

### 2.3 已完成的离线能力

- 项目路径、配置校验、旧配置转换、基础日志和启动器检查。
- 仪器端口协议、领域模型、事件/取消协议、仿真仪器和安全生命周期。
- 线损、驱动映射和主功放统一测量服务，以及同步/Qt 兼容入口的结果回归。
- 运行上下文、连接仪器前快照、共享 `run_id`、运行目录冲突保护和指定运行的基线收集。
- 版本化结果包、旧 JSON 只读读取、CSV/HTML 报告和缺失数据诊断。
- AI、联网搜索、格式转换和 DPD 边界隔离；最小 CI、发布清单和回滚文件清单。

## 3. 目标架构

目标采用简化的六边形架构，依赖只能由外层指向内层：

```text
Qt GUI / CLI
     |
     v
Application services
     |
     v
Domain: plans, run context, results, calculations, safety rules
     |
     +--> Instrument ports --------> VISA / simulation adapters
     +--> Persistence ports -------> JSON / files / future SQLite index
     +--> Analysis ports -----------> plots / reports / exports
```

目标目录语义如下。当前仓库已落地其中的部分边界，旧根目录模块可在迁移期并存；目录搬迁不是重构完成标准。

```text
pa_auto_test/
├── app/                  # 运行上下文、事件、取消和应用服务
├── domain/               # 方案、结果、计算和安全规则
├── instrument/           # 仪器端口、VISA 适配器和仿真
├── persistence/          # 配置、结果和历史格式适配
├── analysis/             # 读取、绘图、报告和导出
├── presentation/qt/      # 页面、窗口和 Qt Worker
├── assistant/            # LLM、联网搜索和聊天历史
├── legacy/               # 过渡包装和旧格式适配器
└── launcher.py
```

## 4. 核心模型和正式链路

系统必须区分以下对象：

- `TestPlan`：稳定的测试方法和参数，不包含现场设备地址。
- `RunContext`：本次运行的设备、物理通道、操作者、接线确认和备注。
- `MeasurementSession`：运行 ID、状态、停止原因、异常和事件流。
- `MeasurementResult`：原始读数、补偿读数、派生指标、单位、方法版本和 schema 版本。

正式运行链路：

```text
加载 TestPlan
  -> 选择或填写 RunContext
  -> 离线校验
  -> 创建运行目录和快照
  -> 创建 InstrumentSession
  -> 执行测量
  -> 保存结果和报告
  -> RF 关闭 -> 电源关闭 -> 连接关闭
```

快照写入失败时，不得连接仪器、上电或打开 RF。

测量服务只依赖仪器端口、事件接收器、取消令牌和结果边界，不依赖 Qt 或 PyVISA。Qt Worker 只负责线程生命周期、信号转换和取消请求。AI、联网搜索、格式转换和 DPD 不得进入主测量初始化路径。

统一安全状态为：

```text
created -> validated -> connected -> prepared -> powered -> measuring
                                                     |
                          stopped / failed ----------+
                                                     v
                                       RF off -> power off -> close
```

清理必须幂等，并尽力执行所有后续动作。普通停止允许完成当前安全边界；紧急停止优先关闭 RF。上电遵守先栅后漏，掉电遵守先漏后栅。

## 5. 不可破坏的业务契约

在真实设备验收之前，不得以架构重构为理由改变：

1. 线损两步路径、衰减器补偿公式和结果字段语义。
2. 驱动映射的扫描范围、输入/输出关系、线损补偿位置和按频率保存方式。
3. 主功放有驱动/无驱动模式、扫描范围、压缩点、DC 功耗和效率计算。
4. DUT 最大输入功率保护触发后不得继续设置更高激励。
5. RF 默认关闭；正常完成、停止、异常和关闭窗口均必须清理。
6. 电源上电和掉电顺序。
7. 旧结果在迁移期间可读，旧启动入口可回滚。
8. 每次运行保存实际方案、资源映射、软件版本、状态和异常信息。

公式、SCPI 命令、安全顺序和结果格式不得在同一批变更中同时修改。

## 6. 验收和发布原则

- 离线验收命令和结果以 `重构执行.md` 为准；当前基线为 294 项测试通过、入口检查通过、配置校验通过。
- 涉及硬件的步骤必须记录设备、地址标识、通道、接线、功率范围、结果差异和清理状态；仿真不能替代真实验收。
- 新实现首次上线必须可切回旧实现。当前回滚边界由 `release_manifest.py` 明确列出。
- 出现测量值异常、保护失效、RF/电源未关闭、结果不可读或 GUI 无法启动时，立即停止切换并回滚。
- 不提交真实 IP、密钥、聊天历史、现场结果和未经脱敏的设备信息。

## 7. 已确定和待确认决策

已确定：`launcher.py` 为目标生产入口；保留 CLI 测量能力；方案与现场资源映射分离；第一版继续以 JSON 保存完整快照和原始结果；`DPD_auto_test/` 不纳入主系统重构；新实现通过兼容包装逐步切换。

待确认：多电源运行的第一版范围、是否增加 SQLite 运行索引、普通停止/紧急停止的现场标准、旧配置兼容期限，以及是否需要队列运行和暂停恢复。
