# PA 自动测试系统重构执行计划

> 本文只记录未完成的重构任务、顺序、验收和回滚要求。
>
> 适用范围：PA 自动测试主系统。`DPD_auto_test/` 暂不纳入本计划，仅在阶段 8 做边界确认。
>
> 当前状态：阶段 0 收尾、阶段 1 接口实现完成，待自动化验收  |  最后更新：2026-09-25

## 1. 执行规则

1. 先建立边界和测试，再迁移生产调用者；不以目录搬迁作为阶段目标。
2. 每次只推进一个小步骤，完成后经过至少一个日常使用周期再进入高风险步骤。
3. 不同时修改测量公式、SCPI 命令、安全顺序和结果格式。
4. 新实现先通过仿真和旧实现对照，再由功能开关切换为默认实现。
5. 真实设备验收必须记录设备、地址、通道、接线、功率范围、结果差异和清理状态。
6. 任何安全、结果正确性或启动问题都暂停后续重构并恢复旧实现。

## 2. 阶段总览

| 阶段 | 主题 | 主要结果 | 风险 |
| --- | --- | --- | --- |
| 0 | 基线冻结和验收样例 | 可比较、可回滚的生产基线 | 低 |
| 1 | 核心接口和事件协议 | 仪器端口、测量服务和停止协议 | 中 |
| 2 | 仿真仪器和安全状态机 | 无真实设备验证核心流程 | 高 |
| 3 | 统一测量服务 | 同步和 Qt 共用同一测量实现 | 高 |
| 4 | 配置和运行上下文迁移 | 新配置成为内部主契约 | 中高 |
| 5 | 结果模型和报告解耦 | 结果长期可读、分析独立 | 中 |
| 6 | GUI 分层和入口收敛 | GUI 只负责展示和交互 | 中 |
| 7 | AI、工具和遗留边界 | 可选功能不影响主测量 | 低至中 |
| 8 | 过渡层清理和发布 | 删除冗余实现，建立发布回滚机制 | 中 |

## 3. 阶段 0：基线冻结和生产验收样例

### 0.1 建立脱敏生产样例

- [ ] 保存最近一次正常运行的测试方案、现场资源映射、线损结果、驱动映射结果、主功放结果和报告索引。
- [ ] 记录 Python、依赖版本、启动方式、仪器型号、VISA 地址标识、电源通道和测试范围。
- [ ] 为三类测量定义最小验收指标：频率点、扫描点、关键结果字段、停止行为和输出状态。
- [ ] 脱敏 IP、密钥、聊天历史、器件信息和现场敏感信息。
- [ ] 由现场人员确认电源通道到驱动功放和 DUT 的实际连接，并记录在样例中。

### 0.2 完成启动和关闭冒烟

- [ ] 手动正常打开并关闭 GUI，确认进程正常退出。
- [ ] 启动和关闭过程中确认 RF 输出和电源输出没有被意外打开。
- [ ] 记录 `launcher.py --check`、`--validate-config` 和普通启动结果。

### 0.3 阶段验收

门槛：有一份脱敏样例；启动和关闭结果可重复；旧入口仍可用；未改变测量公式和硬件动作。

回滚：只保留文档和样例，不切换任何生产实现。

## 4. 阶段 1：定义核心接口和事件协议

目标：先固定依赖边界，暂不改变生产测量行为。

### 1.1 定义仪器端口

- [x] 在 `instrument/ports.py` 定义 `SignalGeneratorPort`。
- [x] 定义 `SpectrumAnalyzerPort`。
- [x] 定义 `PowerSupplyPort`。
- [x] 定义 `InstrumentSession` 的初始化、关闭和状态语义。
- [x] 为每个操作写明单位、前置条件、超时、失败和重试语义。
- [x] 明确电源通道使用物理通道名，供电角色由运行映射提供。

### 1.2 定义领域对象

- [x] 保留旧配置输入模型；新增领域 `TestPlan`，并明确与 `config_models.TestPlan` 的转换留待阶段 4。
- [x] 增加 `RunContext`，明确其与 `RunResourceMapping` 的边界。
- [x] 定义线损、驱动映射、功放扫描点和主功放结果对象。
- [x] 明确原始读数、补偿读数、派生指标和单位。
- [x] 为对象增加 schema 版本和方法版本。

### 1.3 定义测量事件和取消协议

- [x] 定义进度、消息、实时数据、现场确认、完成、停止和失败事件。
- [x] 定义可注入的 `CancellationToken`。
- [x] 区分普通停止和紧急停止。
- [x] 保证事件协议不依赖 Qt。

### 1.4 阶段验收

- [ ] 接口可以被纯 Python 测试导入并通过协议测试（待恢复项目 Python 解释器后执行）。
- [x] 领域模块不导入 PySide6、PyVISA 或具体文件存储模块。
- [x] CLI 和 Qt Worker 的未来调用方式可以使用同一接口表达。

回滚：接口模块作为新增代码保留，不替换旧调用者。

## 5. 阶段 2：实现仿真仪器和安全状态机

目标：在不连接真实设备的情况下验证硬件流程和异常清理。

### 2.1 仿真仪器

- [x] 实现仿真信号源、频谱分析仪和电源。
- [x] 支持固定读数、按顺序读数和基于输入功率的读数模型；支持按通道配置电压/电流读数。
- [x] 记录所有设置、查询、RF 和电源命令。
- [x] 支持设置失败、读数失败、连接失败和掉电失败注入。
- [x] 支持命令序列 JSON 序列化、加载和回放（生产序列需脱敏后使用）。

### 2.2 安全状态机

- [x] 定义并实现 `created`、`validated`、`connected`、`prepared`、`powered`、`measuring`、`stopping`、`cleaned` 状态。
- [x] 统一实现 RF 关闭、电源关闭和连接关闭顺序。
- [x] 清理动作幂等且失败可重试，部分连接失败时仍清理可安全关闭的资源。
- [x] 普通停止要求在测量/供电状态安全边界调用；紧急停止可从活动状态触发并首先关闭 RF。
- [x] 保留“先栅后漏”上电和“先漏后栅”掉电。

### 2.3 阶段验收

- [x] 仿真测试覆盖正常完成、普通停止、紧急停止、异常和窗口关闭。
- [x] 命令记录证明 RF 默认关闭和电源顺序正确。
- [ ] DUT 输入保护触发后不再设置更高激励。
- [x] 仿真适配器不改变现有纯计算实现；全量离线测试通过。

回滚：只切换回旧 `InstrumentControl` 和旧测量入口，不删除仿真实现。

## 6. 阶段 3：统一测量服务

目标：消除同步测量类和增强测量类的核心逻辑复制。

### 3.1 线损服务

- [x] 抽取 `CableLossService`，接收方案、仪器端口、事件接收器和取消令牌。
- [x] 保留路径 1、路径 2 和现场确认检查点。
- [x] 将 `calculate_cable_losses` 作为唯一计算实现。
- [x] 同步入口改为调用服务，旧类保留兼容包装。
- [x] Qt Worker 改为调用同一服务，不再继承旧测量类。

### 3.2 驱动映射服务

- [x] 抽取 `DriverPowerMappingService`。
- [x] 保留功率扫描范围、等待时间、线损补偿和按频率存储语义。
- [x] 实时曲线通过通用数据事件传递。
- [x] 覆盖停止、RF 关闭、驱动电源关闭和异常清理。

### 3.3 主功放服务

- [x] 抽取 `AmplifierMeasurementService`。
- [x] 统一有驱动和无驱动输入功率计算。
- [x] 保留 DUT 最大输入功率保护、压缩点计算、DC 功耗和效率计算。
- [x] 将每个扫描点表示为明确结果对象或受约束的数据结构。
- [x] 覆盖空扫描、未达到压缩点、达到压缩点、停止和读数失败。

### 3.4 阶段验收

- [x] 三类服务不导入 PySide6。
- [x] 同一服务可由 CLI 和 Qt Worker 调用。
- [x] 新旧实现使用相同配置和仿真输入时结果一致。
- [x] 三类服务的清理动作均由统一生命周期控制。

回滚：使用功能开关切回现有三个测量类和 `enhanced_workers.py`。

## 7. 阶段 4：配置和运行上下文迁移

目标：让新配置模型成为内部主契约，旧 `config.json` 仅作为兼容输入。

### 4.1 配置仓储和加载

- [x] 增加测试方案仓储和运行映射仓储；配置仓储组合加载并执行离线校验。
- [x] 校验 schema 版本、模板占位值、仪器、通道和现场接线确认。
- [ ] 运行前只使用本次选定的设备和通道检查安全关系。
- [ ] 不把 `PS4`、固定 IP 或 `CH1`/`CH2` 写入领域逻辑。

### 4.2 旧配置转换

- [x] 旧 `config.json` 经配置仓储转换为 `TestPlan` 和 `RunResourceMapping`。
- [x] 转换产生警告和未解析字段报告。
- [x] 转换过程不连接硬件。
- [ ] 旧启动器行为和 `--validate-config` 继续可用。

### 4.3 运行快照

- [ ] 在连接仪器前创建唯一运行目录。
- [ ] 保存方案、现场映射、软件版本、提交号、时间、状态和异常。
- [ ] 快照写入失败时禁止连接、上电和 RF 操作。
- [x] 运行目录冲突时不覆盖已有数据。
- [x] 快照及测量结果自动带有统一 `run_id`，传入冲突 ID 时拒绝写入。

### 4.4 阶段验收

- [ ] 新配置和旧配置都能生成可读快照。
- [ ] 现场通道映射实际保存在快照中。
- [ ] 不同工作目录运行结果一致。
- [ ] 旧结果读取和旧入口回归通过。

回滚：继续使用旧配置读取和旧测量入口，新快照作为增量功能保留。

阶段 4 当前进度：旧配置到领域快照的生产兼容入口已经经由配置仓储转换；配置仓储、转换审查信息、快照/结果 ID 约束和按指定运行收集已实现。GUI/测量服务仍以旧配置字典作为执行输入，单次跨测量会话的上下文尚未统一，4.3、4.4 的完整验收仍未完成。

## 8. 阶段 5：结果模型和分析解耦

### 5.1 结果模型

- [ ] 定义线损、驱动映射和主功放结果的版本化结构。
- [ ] 明确频率、功率、电压、电流、效率和压缩点单位。
- [ ] 结果关联 `run_id`、方案快照、运行映射快照和方法版本。
- [ ] 区分原始测量、补偿结果、计算指标和报告派生数据。

### 5.2 兼容读写

- [ ] 旧 JSON 使用只读适配器读取。
- [ ] 新格式写入运行目录，并按需保留旧路径兼容副本。
- [ ] 历史文件扫描只报告不可解析文件，不覆盖历史数据。
- [ ] 设计迁移、备份和校验工具。

### 5.3 报告解耦

- [ ] 让分析模块消费结果模型，而不是任意字典。
- [ ] 拆分数据读取、图表生成、CSV 导出、HTML/PDF 组装。
- [ ] 覆盖空结果、缺少压缩点、缺少 DC 数据和字体不可用。

### 5.4 阶段验收

- [ ] 现有样例和新格式结果都能读取。
- [ ] 新旧结果都能生成报告。
- [ ] 报告图表数量和关键字段稳定。

回滚：恢复旧结果读取器和报告入口，新格式文件保留，不批量改写历史结果。

阶段 5 当前进度：已增加线损、驱动映射和主功放结果的领域模型适配入口；历史 JSON 通过只读 `parse_result_model()` / `load_result_model()` 转为版本化模型，扫描点按 Hz、dBm、V、A、W 和百分比字段表达。可视化读取层可接收领域结果模型，同时保留旧字典读取 API。回归覆盖了三类结果、主功放列式扫描和历史文件读取。新模型的规范化写入、方案/资源快照完整关联、CSV/HTML/PDF 组装拆分及字体/缺失 DC 等报告验收尚未完成，因此本阶段仍进行中。

## 9. 阶段 6：GUI 分层和入口收敛

目标：逐步降低 `enhanced_main_gui.py` 的职责和修改风险。

### 6.1 入口收敛

- [x] 将 `launcher.py` 定为生产入口。
- [x] `start_gui.bat` 和 `vscode_launcher.py` 只做兼容转发。
- [ ] 保留 `--check`、`--validate-config` 和一致退出码。
- [x] 更新 README 和启动说明。

### 6.2 Qt Worker 适配层

- [x] Qt Worker 只负责线程生命周期、信号转换和取消请求。
- [x] Worker 不实现测量公式、SCPI 或电源清理。
- [x] 通用事件转换为 Qt Signal。
- [x] 统一完成、停止和失败信号语义。

### 6.3 页面拆分

- [ ] 拆出配置、线损、驱动映射、主功放、可视化和导出页面。
- [x] 建立配置、线损、驱动映射、主功放、可视化和导出页面注册边界。
- [ ] 页面只负责输入、确认、状态展示和事件订阅。
- [ ] 页面不直接读写结果文件、不创建仪器、不调用 SCPI。
- [ ] 保留当前用户流程和关键确认提示。

### 6.4 阶段验收

- [ ] 无真实仪器、无 AI 服务时 GUI 可以启动。
- [ ] 页面测试不需要 PyVISA 连接。
- [ ] Qt 和 CLI 的测量结果及清理行为一致。
- [ ] 正常关闭窗口不会遗留线程、RF 或电源状态。

回滚：保留旧 `enhanced_main_gui.py` 入口和功能开关。

## 10. 阶段 7：AI、工具和 DPD 边界

### 7.1 AI 隔离

- [ ] 将 `llm.py`、`web_search_tools.py`、聊天历史和搜索配置移入可选能力边界。
- [ ] AI 初始化失败不影响主 GUI 和测量启动。
- [ ] 网络搜索失败只影响 AI 功能。
- [ ] API 地址、密钥和聊天历史不进入测量快照和普通日志。

### 7.2 格式转换和连接图

- [ ] 为 `format_convert/` 定义独立入口和输入输出约定。
- [ ] 连接图生成不进入测量核心初始化路径。
- [ ] 明确图片、临时文件和导出文件的目录策略。

### 7.3 DPD 子系统

- [ ] 为 `DPD_auto_test/` 增加独立说明和依赖清单。
- [ ] 禁止其隐式导入或修改 PA 主系统配置、结果和运行目录。
- [ ] 在单独决策前不进行大规模迁移。

### 7.4 阶段验收

- [ ] 主系统在没有 AI、联网搜索和 DPD 依赖时可运行。
- [ ] DPD 目录的输入输出边界有文档记录。

## 11. 阶段 8：过渡层清理和发布机制

- [ ] 统计旧入口、兼容字段、旧类和旧结果适配器的调用者。
- [ ] 至少经过一个稳定生产周期后再删除重复实现。
- [ ] 完成历史结果读取和迁移演练。
- [ ] 增加版本号、变更记录、环境导出、发布包和回滚包。
- [ ] 建立最小 CI：编译、单元测试、仿真集成测试、静态检查和启动冒烟。
- [ ] 记录每类测量的真实设备验收结果。

删除门槛：无调用者、兼容期结束、历史数据可读、回滚包可用、真实设备验收通过。

## 12. 风险登记

| 编号 | 风险 | 处理阶段 | 当前状态 |
| --- | --- | --- | --- |
| R-001 | 依赖未锁定且没有 CI | 8 | 未完成 |
| R-002 | 巨型 GUI 职责混合 | 6 | 未完成 |
| R-003 | 同步/增强测量逻辑重复 | 3 | 未完成 |
| R-004 | 测量模块直接创建仪器控制 | 1、2、3 | 未完成 |
| R-005 | 旧配置和新模型并存 | 4 | 部分完成 |
| R-006 | 结果字段依赖隐式字典键 | 5 | 部分完成 |
| R-007 | 真实 SCPI 行为缺少可重复记录 | 2 | 未完成 |
| R-008 | 异常清理和停止语义需要真实设备确认 | 2、3、6 | 未完成 |
| R-009 | 电源保护参数单位和阈值尚未完成真实确认 | 0、2 | 未完成 |
| R-010 | AI 和搜索服务与 GUI 初始化耦合 | 7 | 未完成 |
| R-011 | 根目录混有运行产物和历史资产 | 5、7 | 部分完成 |
| R-012 | DPD 子系统依赖和边界不清 | 7 | 未完成 |

## 13. 每步完成记录模板

```text
日期：
阶段/步骤：
提交：
涉及文件：
行为变化：
验证命令：
自动化/仿真验证：
真实设备验收：未执行 / 通过 / 失败（附记录）
结果差异：
回滚方式：
风险登记更新：
```
