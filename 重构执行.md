# PA 自动测试系统重构执行文档

> 本文记录当前执行状态、剩余任务、验收和回滚要求。
>
> 适用范围：PA 自动测试主系统。`DPD_auto_test/` 暂不纳入主系统重构。
>
> 最后更新：2026-09-26

## 1. 当前状态

离线可完成事项已收尾。阶段 1、阶段 2 仿真、阶段 3、阶段 4 的离线能力、阶段 5、阶段 6 的入口和 Worker 边界、阶段 7 已完成离线实现与验证。GUI Worker 已在创建兼容适配器前完成 `PreparedRun` 预检和快照；阶段 8 已具备发布/回滚清单和最小 CI。

尚未完成：脱敏生产基线、真实设备验收、稳定生产周期、GUI 页面物理拆分、执行主契约迁移、现场发布包和过渡层清理。指定环境下 294 项测试通过，`launcher.py --check`、`launcher.py --validate-config` 通过。

## 2. 执行规则

1. 先建立边界和测试，再迁移生产调用者；不以目录搬迁作为阶段目标。
2. 每次只推进一个小步骤，完成后经过至少一个日常使用周期再进入高风险步骤。
3. 不同时修改测量公式、SCPI 命令、安全顺序和结果格式。
4. 新实现先通过仿真和旧实现对照，再由兼容入口或功能开关切换。
5. 真实设备验收必须记录设备、地址、通道、接线、功率范围、结果差异和清理状态。
6. 任何安全、结果正确性或启动问题都暂停后续重构并恢复旧实现。

## 3. 阶段状态

| 阶段 | 主题 | 当前状态 | 下一门槛 |
| --- | --- | --- | --- |
| 0 | 基线冻结和现场样例 | 未完成 | 同一 `run_id` 的脱敏完整运行基线 |
| 1 | 核心接口和事件协议 | 已完成离线验证 | 真实设备端口验收 |
| 2 | 仿真仪器和安全状态机 | 已完成离线验证 | 真实设备保护和清理验收 |
| 3 | 统一测量服务 | 核心逻辑已统一，旧适配器保留 | 生产调用者迁移和结果对照 |
| 4 | 配置和运行上下文迁移 | 预检、快照和 ID 约束已接入 | 执行输入迁移为领域模型 |
| 5 | 结果模型和报告解耦 | 已完成离线验证 | 现场结果差异和生产周期 |
| 6 | GUI 分层和入口收敛 | 入口/Worker 边界完成，页面未物理拆分 | 页面职责迁移和 GUI 现场验证 |
| 7 | AI、工具和遗留边界 | 离线部分完成，兼容 shim 保留 | 稳定周期后清理 shim |
| 8 | 过渡层清理和发布 | 清单/CI 已有，发布门槛未满足 | 真实验收、稳定周期和回滚包 |

## 4. 剩余执行任务

### 4.1 阶段 0：脱敏生产基线

- [ ] 使用同一个 `run_id` 完成一次完整的线损、驱动映射和主功放运行，保存方案、资源映射、三类结果和报告索引。
- [ ] 记录 Python、依赖版本、启动方式、仪器型号、VISA 地址标识、电源通道和测试范围。
- [ ] 定义三类测量的频率点、扫描点、关键字段、停止行为和输出状态。
- [ ] 脱敏 IP、密钥、聊天历史、器件信息和现场敏感信息。
- [ ] 由现场人员确认电源通道到驱动功放和 DUT 的实际连接。
- [ ] 手动打开并关闭 GUI，确认进程、Worker、RF 和电源状态正常。

阶段门槛：有一份完整脱敏样例；启动和关闭可重复；旧入口仍可用；公式和硬件动作未改变。

### 4.2 真实设备验收

- [ ] 为线损、驱动映射和主功放分别记录设备型号、地址标识、通道、接线、功率范围和关键结果。
- [ ] 验证 RF 默认关闭、普通停止、紧急停止、窗口关闭和异常清理。
- [ ] 验证 DUT 输入保护触发后不再设置更高激励。
- [ ] 验证先栅后漏上电、先漏后栅掉电以及清理失败后的继续清理行为。
- [ ] 用现场结果与历史基线比较，并记录可解释的结果差异。

### 4.3 生产迁移和清理

- [ ] 将 GUI/CLI 执行输入从旧配置字典迁移为 `TestPlan`、`RunContext` 和统一 `MeasurementSession`。
- [ ] 将真实 VISA/SCPI 行为接入端口适配器并完成设备验收，保持现有命令和安全顺序。
- [ ] 完成配置、线损、驱动映射、主功放、可视化和导出页面的物理拆分；页面只提交操作、订阅事件和展示状态。
- [ ] 经过至少一个稳定生产周期后，统计兼容 shim 和旧入口调用者。
- [ ] 仅在无调用者、兼容期结束、历史数据可读、回滚包可用且真实设备验收通过时删除旧实现。
- [ ] 生成现场发布包和回滚包，更新 `release_manifest.py` 中的验收状态。

## 5. 已完成离线能力记录

- `instrument/ports.py`、`app/events.py`、`app/cancellation.py` 和领域模型已建立。
- `instrument/simulation.py` 和生命周期清理已覆盖正常、停止、异常和窗口关闭场景。
- 三类测量服务已统一核心编排，同步入口和 Qt Worker 均通过兼容适配器调用。
- 配置仓储、旧配置转换、运行快照、共享 `run_id` 和指定运行基线收集已实现。
- 版本化结果、旧 JSON 只读读取、CSV/HTML 报告和可选能力边界已实现。
- `launcher.py` 已作为目标生产入口，旧入口和旧测量实现仍保留；CI、发布清单和回滚文件清单已建立。

## 6. 验证命令

执行前必须确认指定解释器存在，不得改用 base 环境或裸 `python`：

```powershell
Test-Path -LiteralPath "D:\Anaconda\envs\Auto_test\python.exe"
& "D:\Anaconda\envs\Auto_test\python.exe" -m unittest discover -s tests -v
& "D:\Anaconda\envs\Auto_test\python.exe" launcher.py --check
& "D:\Anaconda\envs\Auto_test\python.exe" launcher.py --validate-config
```

涉及真实设备时，还必须记录设备型号、地址标识、使用通道、接线确认、功率范围、停止/清理结果和关键结果差异。

## 7. 回滚边界

在真实设备验收和一个稳定生产周期完成前，不删除以下文件：

```text
enhanced_main_gui.py
enhanced_workers.py
cable_loss_measurement.py
driver_power_mapping.py
amplifier_measurement.py
instrument_control.py
```

出现测量值异常、DUT 保护失效、RF 或电源未关闭、结果不可读、报告无法生成、GUI 无法启动或线程无法退出时，停止后续重构，恢复旧入口和旧适配器；新格式结果文件保留，不批量改写历史数据。

## 8. 风险登记

| 编号 | 风险 | 当前状态 |
| --- | --- | --- |
| R-001 | 依赖未锁定 | CI 已安装 `requirements.txt`，依赖仍未锁定 |
| R-002 | 巨型 GUI 职责混合 | 部分缓解：页面注册边界已建立，物理拆分未完成 |
| R-003 | 同步/增强测量逻辑重复 | 已缓解：核心逻辑集中到服务，旧适配器仍保留 |
| R-004 | 测量模块直接创建仪器控制 | 部分缓解：已有端口协议，生产 VISA 适配仍未验收 |
| R-005 | 旧配置和新模型并存 | 部分完成：预检和快照使用新模型，执行输入仍有旧字典 |
| R-006 | 结果字段依赖隐式字典键 | 部分完成：版本化模型和读取适配已存在，旧服务结果仍保留字典 |
| R-007 | 真实 SCPI 行为缺少可重复记录 | 未完成：当前只有仿真命令记录 |
| R-008 | 异常清理和停止语义缺少现场确认 | 未完成 |
| R-009 | 电源保护参数和阈值缺少真实确认 | 未完成 |
| R-010 | AI 和搜索服务与 GUI 初始化耦合 | 已缓解：按需导入并提供降级 |
| R-011 | 根目录混有运行产物和历史资产 | 部分完成：可选能力已隔离，旧兼容模块仍在根目录 |
| R-012 | DPD 子系统依赖和边界不清 | 已缓解：独立说明和依赖清单已建立 |

## 9. 执行记录模板

```text
日期：
阶段/步骤：
提交：
涉及文件：
结构边界变化：
行为变化：
验证命令：
仿真/自动化结果：
真实设备验收：未执行 / 通过 / 失败
结果差异：
回滚方式：
风险登记更新：
```
