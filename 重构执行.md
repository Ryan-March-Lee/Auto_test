# PA 自动测试系统重构执行清单

> 本文只记录尚未完成的重构阶段、步骤、验收和遗留问题。完成项应在每次重构后从这里移除，并同步写入 `重构基线.md` 的里程碑记录。
>
> 当前状态：阶段 0 执行中  |  最后更新：2026-08-19  |  生产频率：每天一次
>
> 配置决策：采用“测试方案模板 + 单次运行资源映射 + 结果快照”。电源设备、IP 地址和现场通道接线允许每次测试变化，不作为永久固定生产配置。

## 使用规则

- 每次只推进一个阶段中的一个小步骤，完成后先经过至少一次日常使用周期。
- 标记为“硬件验收”的步骤不得仅用编译或仿真结果代替。
- 每个步骤完成时补充：日期、提交、验证命令、结果差异、回滚结论。
- 发现影响安全、数据正确性或启动的遗留问题，先暂停后续重构并记录在文末。
- 默认不删除旧入口、旧结果或旧字段；删除必须有迁移证明和回滚方案。

## 阶段 0：基线冻结与可观测性

目标：在不改变测量行为的前提下，建立可比较、可回滚的生产基线。

### 0.1 建立生产验收样例

- [ ] 从最近一次正常使用中保存一份脱敏测试方案快照、单次运行资源映射快照、线损 JSON、驱动映射 JSON、主功放 JSON 和报告索引。
- [ ] 记录实际使用的启动方式、Python/Conda 环境、依赖版本、当次仪器型号/VISA 地址标识、电源通道映射和测试参数；不要求下一次复用这些地址或通道。
- [ ] 为每类测量定义最小验收指标：频率点数量、扫描点数量、结果关键字段、异常停止行为和输出关闭状态。
- [ ] 对样例中的真实 IP、API 地址、密钥、聊天历史和器件敏感信息做脱敏；确认原始文件不新增到 Git。
- [ ] 明确每次测试开始前由现场人员确认电源通道到驱动功放和 DUT 的实际连接，并将确认结果写入运行快照。

### 0.2 统一启动冒烟检查

- [ ] 手动正常关闭一次 GUI，并确认进程正常退出。
- [ ] 在不执行测量的前提下，确认 GUI 启动和关闭均未改变 RF 输出、电源输出或仪器连接状态。

### 0.3 建立最小日志与错误记录

- [ ] 区分用户可见消息、调试日志、硬件命令日志和异常堆栈，先不改变现有界面文案。
- [ ] 每次测量记录开始、结束、停止、失败、RF 关闭、电源关闭和结果保存事件。
- [ ] 设计日志脱敏规则，不记录 API 密钥和不必要的局域网凭据。

验收门槛：旧 GUI 启动方式和现有真实测量结果不变；有一份脱敏基线样例；异常时能证明 RF 和电源清理路径被执行。

## 阶段 1：配置、路径与依赖治理

目标：固定输入契约，降低字符串字典和机器环境差异带来的风险。

### 1.1 配置校验与兼容读取

- [ ] 将 `config.json` 拆分为可版本化的测试方案和可每次覆盖的运行资源配置；增加两者的持久化版本字段和兼容读取规则。
- [ ] 允许在 GUI 测试开始前填写或选择当前仪器地址、电源设备和通道映射；不把设备地址或物理接线当作永久固定配置。
- [ ] 在连接/上电前校验本次映射：设备和通道存在、通道不重复、驱动/DUT 分配完整、保护参数完整、现场接线已确认。

### 1.2 路径与运行目录

- [ ] 继续以 `project_paths.py` 为过渡单一入口，补充测试方案目录、运行快照目录、数据目录、报告目录和临时目录的语义。
- [ ] 区分源代码、用户配置、测量结果、报告、缓存和聊天历史；确认现有文件的迁移和备份策略。
- [ ] 消除模块内部直接拼接项目根目录的新增用法，统一使用路径服务。

### 1.3 依赖和环境

- [ ] 从当前 `Auto_test` 环境导出经过确认的依赖清单和 Python 版本。
- [ ] 增加项目级依赖声明或锁定文件，区分 GUI、硬件、报告、AI、DPD 可选依赖。
- [ ] 为无真实仪器的开发环境定义安装和启动方式。

验收门槛：错误配置在连接仪器前被拒绝；不同工作目录启动结果一致；新环境能完成编译和无硬件启动检查。

## 阶段 2：仪器端口与仿真仪器

目标：把 PyVISA 细节和硬件状态管理从测量计算中隔离出来。

### 2.1 定义仪器能力接口

- [ ] 抽象信号源、频谱分析仪、电源和资源生命周期接口；接口实例由本次运行的设备地址和通道映射创建。
- [ ] 将 `set_frequency`、`set_power`、RF 开关、频谱测量、电压/电流读取、电源通道开关等动作定义为明确操作。
- [ ] 为每个动作定义单位、前置状态、超时、失败语义和是否允许重试。
- [ ] 保留当前 SCPI 命令作为 PyVISA 适配器实现，先不改变命令内容。

### 2.2 建立仿真/录制回放

- [ ] 实现不连接真实设备的仿真仪器，支持固定读数、按输入变化读数、故障注入和命令记录。
- [ ] 覆盖 RF 默认关闭、停止时关闭、异常时关闭、电源上下电顺序和 DUT 保护触发。
- [ ] 让测量服务可以通过构造参数注入真实适配器或仿真适配器，并在运行时注入当前现场资源映射。
- [ ] 以当前生产样例建立命令序列对照，不把仿真值当成真实精度证明。

### 2.3 资源清理和安全状态机

- [ ] 将测量生命周期统一为准备、上电、射频扫描、停止/失败、掉电、关闭连接几个状态。
- [ ] 保证清理动作幂等；仪器未连接、部分连接或某一步失败时仍能继续关闭其他资源。
- [ ] 明确紧急停止与普通停止的差异，并让 GUI 和同步入口调用同一个停止协议。
- [ ] 真实设备上验证安全顺序，记录操作者和结果。

验收门槛：全部仿真安全测试通过；真实设备在低功率、短扫描范围下通过硬件验收；任何失败都可回退旧实现。

## 阶段 3：测量领域服务抽离

### 已完成的前三项低风险重构（2026-08-21）

以下范围已完成，并保留公有入口、结果字段和硬件动作顺序：

- GUI 配置读写与 UI 同步逻辑已拆分。
- 仪器初始化、上电和掉电安全流程已拆分。
- 新增 `measurement_calculations.py`，提取线损、功率补偿、驱动插值、DUT 输入功率、增益、效率和压缩点计算。
- 同步入口和增强入口均已接入共享纯函数。
- 新增纯计算测试和测量循环回归测试，覆盖同步/增强结果一致性、硬件动作顺序和 DUT 输入保护时机。

验证记录：

- `Auto_test` 编译检查通过。
- `tests.test_measurement_calculations` 与 `tests.test_measurement_loop_regression` 共 52 项通过。
- 全量测试共 187 项，其中 2 项因工作区 `config.json` 的驱动模式状态与测试预期不一致而失败；未修改现场配置或测试以规避失败。
- 真实设备验收尚未执行。

以上完成项不等于本阶段后续“统一测量服务对象”和“消除同步/增强流程复制”的大范围重构已完成；这些事项仍按下方清单保留。

目标：统一线损、驱动映射和主功放的核心流程，消除同步版与增强版重复。

### 3.1 先抽纯计算规则

- [ ] 将线损补偿、驱动功率补偿、主功放输出补偿、效率、增益、压缩点和插值提取为无硬件依赖函数/对象。
- [ ] 使用生产样例和边界输入建立单元测试，包含空结果、未达到压缩点、超限停止、频率键格式和浮点排序。
- [ ] 明确公式中的参考点、单位和字段命名，结果中保留必要的计算元数据。

### 3.2 统一测量服务

- [ ] 将 `CableLossMeasurement`、`DriverPowerMapping`、`AmplifierMeasurement` 的硬件动作编排重构为服务对象。
- [ ] 让同步 CLI 和 Qt 工作线程都调用同一个服务，不再通过继承业务类复制并改写整段流程。
- [ ] 用事件/回调报告进度、消息、数据点和可停止状态；业务服务不直接依赖 Qt。
- [ ] 逐个切换线损、驱动映射、主功放用例，切换前后用同一配置和仿真输入比较结果。

### 3.3 主功放专项安全验收

- [ ] 验证无驱动和驱动模式的 DUT 输入功率计算分别正确。
- [ ] 验证达到 `max_input_power` 后不会继续设置更高激励。
- [ ] 验证压缩点不足、刚好达到和达到后停止三类场景。
- [ ] 验证 DC 电源读取异常、频谱测量异常和用户停止时的 RF/电源清理。

验收门槛：纯计算测试通过；仿真结果与旧实现在允许误差内一致；三类测量各完成一次低风险真实设备验收。前三项低风险范围的自动化和仿真门槛已通过，但真实设备验收仍未完成。

## 阶段 4：结果模型与持久化迁移

目标：使结果可追溯、可验证、可长期读取。

### 4.1 结果模型

- [ ] 定义线损、驱动映射、主功放结果的版本化结构、单位、频率键和时间格式。
- [ ] 增加运行 ID、测试方案快照、单次运行资源映射快照、软件版本/提交标识、测量状态和异常信息。
- [ ] 区分原始读数、补偿后读数、计算指标和报告派生数据。

### 4.2 兼容迁移

- [ ] 实现旧 JSON 读取适配器和新模型读取接口。
- [ ] 新结果先写新格式，同时按需要生成旧格式兼容副本；确认报告和 GUI 均可读。
- [ ] 对历史根目录结果进行只读扫描，报告无法解析的文件而不是自动覆盖。
- [ ] 设计迁移工具、备份、校验和回滚说明，禁止在首次上线时批量破坏性改写历史文件。

### 4.3 报告解耦

- [ ] 让 `data_visualization.py` 消费结果模型而不是直接依赖任意字典结构。
- [ ] 将图表生成、CSV 导出和 HTML 组装拆成可独立测试的步骤。
- [ ] 处理空图、缺少压缩点、缺少电压/电流列和 Windows 字体不可用等情况。

验收门槛：全部现有样例可读取；新旧结果都能生成报告；报告文件引用和图表数量稳定。

## 阶段 5：GUI 分层与入口收敛

目标：逐步降低 `enhanced_main_gui.py` 的职责和修改风险。

### 5.1 入口收敛

- [ ] 选定一个生产入口，其他入口改为兼容包装并输出一致的错误码/日志。
- [ ] 保留 `launcher.py --check`，让 GUI 启动不再承担复杂环境决策。
- [ ] 修正 README 中不存在的文件和过时启动说明，增加重构后实际入口说明。

### 5.2 页面与用例适配

- [ ] 按配置、线损、驱动映射、主功放、可视化、导出、日志、AI 助手拆分页面组件。
- [ ] 页面只负责输入校验、状态展示、用户确认和事件订阅；不直接读写结果文件和执行 SCPI。
- [ ] 使用 Qt 线程/任务适配器调用阶段 3 的服务，统一停止、完成和失败信号。
- [ ] 保留当前用户流程和关键确认提示，避免在结构重构中引入交互变化。

### 5.3 AI 与可选功能隔离

- [ ] 将 `llm.py`、`web_search_tools.py`、聊天历史和 API 配置移出主测量初始化路径。
- [ ] AI 服务不可用时 GUI 和测量仍正常启动；联网搜索失败只影响 AI 回复。
- [ ] 明确历史、API 密钥、局域网地址的存储权限和脱敏日志规则。

验收门槛：无 AI/无联网时主测量流程可用；GUI 页面测试不需要真实仪器；日常操作路径与旧版本一致。

## 阶段 6：遗留工具和 DPD 子系统隔离

- [ ] 给 `format_convert/` 增加独立入口、依赖说明和输入输出约定。
- [ ] 将 `DPD_auto_test/` 标记为独立实验性子系统，记录 Notebook、TensorFlow、SciPy、nptdms 等依赖。
- [ ] 禁止 DPD 模块隐式导入或修改 PA GUI 的配置、结果和运行目录。
- [ ] 为 DPD 选择后续策略：独立项目、只读归档或单独测试；在未决策前不做大规模迁移。

## 阶段 7：过渡层清理与发布机制

- [ ] 统计旧入口、兼容字段、旧结果适配器和旧类的调用者。
- [ ] 至少经过一个稳定日常使用周期和一次历史数据迁移演练后，才删除过渡实现。
- [ ] 建立版本号、变更记录、发布包/环境导出、回滚包和现场验收模板。
- [ ] 建立最小 CI：编译、单元测试、仿真集成测试、静态检查和启动冒烟。

## 遗留问题与风险登记

| 编号 | 问题 | 影响 | 临时措施 | 处理阶段 | 状态 |
| --- | --- | --- | --- | --- | --- |
| R-001 | 已有基础自动化测试，但没有依赖锁定和 CI | 环境漂移或合并后回归仍可能未被及时发现 | 当前通过 `Auto_test` 本地执行 31 项测试；后续补依赖锁定和 CI | 1、7 | 未完成 |
| R-002 | `enhanced_main_gui.py` 约 3548 行，职责混杂 | GUI 改动回归范围大 | 暂不整体重写，先通过服务接口减压 | 3、5 | 未完成 |
| R-003 | 同步测量类与 `enhanced_workers.py` 重复核心逻辑 | 行为可能漂移 | 切换前做旧/新结果对照 | 3 | 未完成 |
| R-004 | 测量模块直接创建 `InstrumentControl` | 无法脱离真实硬件测试 | 先定义端口并注入仿真适配器 | 2 | 未完成 |
| R-005 | 配置仍是无版本 schema 的 JSON，且测试方案与现场资源映射混在一起 | 版本迁移、设备更换或通道变化可能引入错误 | 已在启动前执行只读校验；后续拆分测试方案/运行配置并增加版本字段，运行前确认当前映射 | 1、2 | 未完成 |
| R-006 | 结果文件格式无版本，字段依赖隐式字典键 | 历史结果和报告易损坏 | 旧格式只读兼容，先不删除文件 | 4 | 未完成 |
| R-008 | AI/搜索服务含固定局域网地址和可选密钥配置 | 隐私、可用性和启动耦合风险 | AI 视为可选能力，不进入测量核心 | 5 | 未完成 |
| R-009 | `DPD_auto_test/` 依赖重、Notebook 多、边界不清 | 可能污染主系统环境 | 先隔离，不与主链路并行重构 | 6 | 未完成 |
| R-010 | 根目录混有历史 JSON、MDF、图表和临时资产 | 备份、审查和发布边界不清 | 先只读盘点，禁止批量移动覆盖 | 0、1 | 未完成 |
| R-011 | 真实设备行为和 SCPI 命令缺少可重复记录 | 硬件重构风险高 | 建立命令记录与仿真回放 | 0、2 | 未完成 |
| R-012 | `instrument_control.py` 中部分异常处理、清理和测量返回语义不明确 | 异常时可能掩盖故障 | 先补日志和仿真故障注入，再调整行为 | 0、2 | 未完成 |
| R-013 | 电源保护参数的 SCPI 单位和阈值语义尚未完成真实设备确认，且电源设备可能更换 | 可能误判保护关系或遗漏设备风险 | 仅对本次运行选定的电源和通道产生保护关系警告，不阻断启动；真实仪器确认后再决定是否升为错误 | 0、1、2 | 未完成 |
| R-014 | GUI 已完成模块导入和短时窗口启动冒烟，但尚未完成正常窗口关闭及仪器状态验收 | 可能存在 Qt 运行期清理或硬件状态问题 | 使用 `Auto_test` 完成了 8 秒窗口存活检查并强制结束进程；后续手动关闭窗口且确认 RF/电源状态 | 0、2 | 未完成 |

## 每次重构完成记录模板

在完成一个步骤后，复制以下模板填写并将结果同步到基线文档：

```text
日期：
阶段/步骤：
提交：
涉及文件：
行为变化：
验证命令：
仿真验证：
真实设备验收：未执行 / 通过 / 失败（附记录）
结果差异：
回滚方式：
遗留问题更新：
```
