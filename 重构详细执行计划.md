# PA 自动测试系统重构详细执行计划

> 文档性质：长期执行计划。
>
> 本文把 `重构基线.md` 的阶段路线拆解为可逐项执行的小任务，供后续慢速推进使用。
> 本文不替代 `重构执行.md`：具体未完成事项和验收结果仍记录在 `重构执行.md`，阶段完成后同步更新 `重构基线.md`。
>
> 当前起点：已确定采用“测试方案模板 + 单次运行资源映射 + 结果快照”模型；尚未实现新的配置模型、运行配置校验和快照写入代码。

## 1. 总体目标

逐步实现以下运行链路：

```text
加载测试方案模板
    -> 填写或选择本次仪器和电源资源
    -> 填写本次电源通道映射
    -> 确认现场接线
    -> 校验本次运行配置
    -> 校验通过后连接仪器
    -> 生成运行快照
    -> 执行测量
    -> 保存结果和报告
```

核心约束：

- 电源设备可能更换。
- 仪器 IP 地址可能变化。
- 电源通道到驱动功放、DUT 的物理连接由现场决定。
- 不把设备地址和现场接线关系当作永久固定配置。
- 一台电源的不同通道必须分别记录电压、电流和保护参数。
- 配置重构期间不修改测量公式和硬件安全行为。
- 旧 `config.json`、旧结果 JSON 和旧启动入口保持兼容。

## 2. 配置模型

### 2.1 测试方案模板

文件：`baseline/test_plan.json`

保存相对稳定的测试方法和参数：

- 测试频率范围、单位和排序规则。
- 信号源起始功率、终止功率、步进和单位。
- 压缩点类型。
- 衰减器类型或衰减值。
- DUT 最大输入功率。
- DUT 实际使用的每个电源通道的电压、电流和保护参数。
- 驱动模式开关。
- 驱动功放实际使用的每个电源通道参数。
- 测量方法或公式版本。
- 其他稳定的测试参数。

模板可以包含空值，但不能直接作为正式运行配置使用。

### 2.2 单次运行资源映射

文件：`baseline/run_mapping.json`

保存每次测试前由现场确定的信息：

- 测试时间和操作者。
- 当前信号源型号和 VISA 地址。
- 当前频谱仪型号和 VISA 地址。
- 当前直流电源型号和 VISA 地址。
- 驱动功放是否启用。
- 驱动功放使用的电源通道。
- DUT 使用的电源通道。
- 每个通道实际连接对象。
- 现场接线说明。
- 接线是否已经由操作者确认。
- 本次测试备注。

运行映射不应假定固定的电源名称、IP 地址、通道数量或通道编号。

### 2.3 结果运行快照

每次正式运行都要保存当时的完整配置副本：

```text
test_results/<run_id>/
├── test_plan_snapshot.json
├── run_mapping_snapshot.json
├── run_metadata.json
├── cable_loss/
├── driver_mapping/
├── amplifier_measurement/
└── report/
```

快照必须记录：

- 本次实际使用的测试参数。
- 本次实际使用的设备和 VISA 地址。
- 本次实际使用的电源通道映射。
- 现场接线说明和确认状态。
- 软件版本或 Git 提交号。
- 运行 ID、开始时间、结束时间和最终状态。
- 异常信息和停止原因。

## 3. 阶段 A：整理模板和目录

目标：把配置概念固定下来，不改变现有测量行为。

### A.1 整理模板文件

- [x] 将 `baseline/test_plan.json` 保持为纯模板。
- [x] 将 `baseline/run_mapping.json` 保持为纯模板。
- [x] 模板中的空字段使用 `null`、空数组或空对象表示。
- [ ] 不在模板中填入虚构的日期、操作者、仪器地址或测试结果文件。
- [x] DUT 和驱动功放的电源参数按稳定供电角色保存，并由运行映射绑定现场通道。
- [x] 不使用固定的 `CH1`、`CH2`、`CH3`、`CH4` 作为程序前提。
- [ ] 为模板增加简短填写说明。

### A.2 确认目录语义

- [ ] 确认 `baseline/` 只存放模板和脱敏基线资料。
- [ ] 确认运行快照存放在 `test_results/<run_id>/`。
- [ ] 确认测试方案模板、用户填写的运行配置和正式结果的备份策略。
- [ ] 确认真实 IP、密钥和敏感信息不进入 Git。
- [ ] 检查 `.gitignore` 是否覆盖缓存、临时文件和本地敏感配置。

### A.3 阶段 A 验收

- [x] 两个模板都是有效 JSON。
- [ ] 模板不包含某次真实测试的假定数据。
- [x] 模板结构能够表达一台电源的多个通道。
- [ ] 模板结构能够表达多个电源设备的可能性，或已明确当前版本只支持一台电源并留下扩展点。
- [ ] 文档中不再把完整现场配置描述为固定生产配置。

## 4. 阶段 B：实现配置数据模型

目标：用明确的数据对象替代测量代码中直接使用的任意字典。

### B.1 新增模块

建议新增：

```text
config_models.py
```

建议定义：

- `TestPlan`
- `PowerChannelPlan`
- `RunResourceMapping`
- `InstrumentMapping`
- `ChannelMapping`
- `RunConfiguration`
- 配置校验结果对象和问题对象

优先使用标准库 `dataclasses`，暂不为配置模型引入新的第三方依赖。

### B.2 实现读取接口

建议提供以下接口：

```python
load_test_plan(path) -> TestPlan
load_run_mapping(path) -> RunResourceMapping
load_run_configuration(plan_path, mapping_path) -> RunConfiguration
```

读取时必须：

- 检查文件存在。
- 检查 JSON 格式。
- 检查 `schema_version`。
- 识别 `template: true`。
- 禁止模板占位字段进入正式运行。
- 将 JSON 转换为明确的数据对象。
- 保留原始数据副本，供快照写入使用。

### B.3 测试方案校验

#### 频率

- [ ] 频率列表不能为空。
- [ ] 所有频率必须是有限数值。
- [ ] 频率必须大于零。
- [ ] 频率不能重复。
- [ ] 频率排序规则明确且测试覆盖。

#### 功率扫描

- [ ] 起始功率、终止功率和步进不能为空。
- [ ] 起始功率不能大于终止功率。
- [ ] 步进必须大于零。
- [ ] 所有功率必须是有限数值。

#### 压缩点和衰减器

- [ ] 压缩点只允许已定义的值，例如 `1dB`、`3dB`、`5dB`。
- [ ] 衰减器数值必须有效。
- [ ] 衰减器 dB 值不能为负数。

#### DUT 和电源通道

- [ ] `max_input_power` 必须存在且为有限数值。
- [ ] 每个实际使用的电源通道都必须单独填写电压、电流和保护参数。
- [ ] 不允许自动复制其他通道参数。
- [ ] 通道名、连接对象和电气参数的关系明确。

#### 驱动模式

- [ ] 驱动模式关闭时，驱动通道可以为空。
- [ ] 驱动模式开启时，至少存在一个驱动功放通道。
- [ ] 驱动模式开启时，驱动通道参数必须完整。

### B.4 运行映射校验

#### 仪器

- [ ] 信号源型号和 VISA 地址必须填写。
- [ ] 频谱仪型号和 VISA 地址必须填写。
- [ ] 电源型号和 VISA 地址必须填写。
- [ ] 地址不能是空值、`无`、`待填写`或模板占位值。
- [ ] 第一版只做必要的非空校验，不限制具体 VISA 地址格式。

#### 通道

- [ ] DUT 至少分配一个电源通道。
- [ ] 每个通道必须有实际通道名称。
- [ ] 每个通道必须有连接对象。
- [ ] 同一个物理通道不能同时分配给 DUT 和驱动功放。
- [ ] 驱动模式关闭时，驱动通道必须为空。
- [ ] 驱动模式开启时，驱动通道必须完整。
- [x] 程序不得假定通道一定是 `CH1` 到 `CH4`。

#### 安全确认

- [ ] `wiring.confirmed` 为 `true` 才能进入连接或上电流程。
- [ ] 接线说明不能为空。
- [ ] 接线确认表示现场人员确认映射，不代表程序自动判断了物理连接。

### B.5 阶段 B 验收

- [x] 模板可以被读取为数据对象。
- [x] 模板不能直接通过正式运行校验。
- [x] 合法的完整运行配置可以通过校验。
- [x] 缺失字段能返回字段路径和明确错误信息。
- [x] 配置校验不连接真实仪器。

## 5. 阶段 C：兼容现有配置

目标：保留旧系统可启动和旧数据可读取的能力。

### C.1 兼容旧 `config.json`

新增转换接口：

```python
legacy_config_to_test_plan(config) -> TestPlan
legacy_config_to_run_mapping(config) -> RunResourceMapping
```

转换原则：

- [ ] 频率、功率范围、压缩点和衰减器转换为测试方案。
- [ ] 仪器地址转换为默认运行映射，仅作为默认值。
- [ ] 旧电源分配转换为历史默认映射。
- [ ] 不把 `PS4` 或其他设备名称视为永久设备。
- [ ] 旧配置中驱动模式和驱动通道不一致时给出明确提示。
- [ ] 当前运行开启驱动模式但未填写驱动通道时，仍然阻止连接或上电。

### C.2 保持启动器兼容

以下命令的现有含义保持不变：

```powershell
python launcher.py --check
python launcher.py --validate-config
python launcher.py
```

- [ ] `--check` 行为不变。
- [ ] `--validate-config` 继续校验旧默认配置。
- [ ] 新运行配置校验先通过独立模块测试，不立即改变启动器流程。
- [ ] 旧启动函数调用形态继续可用。

### C.3 阶段 C 验收

- [ ] 当前 `config.json` 可以读取。
- [ ] 旧启动器测试全部通过。
- [ ] 旧结果 JSON 不受影响。
- [ ] 转换过程不连接真实 VISA 仪器。
- [ ] 旧入口仍可以回滚使用。

## 6. 阶段 D：实现运行快照

目标：让每次测试都能追溯当时实际使用的配置。

### D.1 生成运行 ID

建议格式：

```text
YYYYMMDD-HHMMSS-随机后缀
```

- [ ] 同一天多次运行不会产生相同 ID。
- [ ] 运行 ID 可以作为结果目录名。
- [ ] 运行 ID 写入所有结果元数据。

### D.2 创建结果目录

建议目录：

```text
test_results/<run_id>/
├── test_plan_snapshot.json
├── run_mapping_snapshot.json
├── run_metadata.json
├── cable_loss/
├── driver_mapping/
├── amplifier_measurement/
└── report/
```

- [ ] 创建目录失败时不启动测量。
- [ ] 快照写入失败时不启动上电或 RF。
- [ ] 不覆盖已有运行目录。

### D.3 写入快照

`test_plan_snapshot.json` 包含：

- 本次实际测试参数。
- 每个实际使用电源通道的参数。
- 方案版本和测量方法版本。

`run_mapping_snapshot.json` 包含：

- 本次仪器型号和地址。
- 本次电源设备和通道映射。
- 现场接线说明。
- 操作者和时间。
- 接线确认状态。

`run_metadata.json` 包含：

```json
{
  "run_id": "...",
  "software_version": "...",
  "git_commit": "...",
  "start_time": "...",
  "end_time": null,
  "status": "created",
  "error": null
}
```

### D.4 阶段 D 验收

- [ ] 每次运行生成唯一目录。
- [ ] 快照可以重新读取。
- [ ] 快照中保留实际通道映射。
- [ ] 快照中不存在模板占位字段。
- [ ] 写入失败时返回明确错误。
- [ ] 写入失败时不覆盖已有结果。

## 7. 阶段 E：补充自动化测试

建议新增：

```text
tests/test_config_models.py
```

### E.1 测试方案测试

- [x] 空频率列表失败。
- [x] 负频率失败。
- [x] 重复频率失败。
- [x] 起始功率大于终止功率失败。
- [x] 零步进失败。
- [x] 非法压缩点失败。
- [x] 缺少 DUT 最大输入功率失败。
- [x] 不同通道可以保存不同电气参数。
- [x] 驱动模式关闭且无驱动通道时通过。
- [x] 驱动模式开启但无驱动通道时失败。

### E.2 运行映射测试

- [x] 缺少信号源地址失败。
- [x] 缺少频谱仪地址失败。
- [x] 缺少电源地址失败。
- [x] DUT 通道为空失败。
- [x] DUT 和驱动功放使用重复通道失败。
- [x] 驱动模式关闭且无驱动通道时通过。
- [x] 驱动模式开启但无驱动通道时失败。
- [x] 接线未确认时失败。
- [x] 任意合法通道名可以通过，不固定 `CH1` 到 `CH4`。

### E.3 兼容测试

- [ ] 当前旧版 `config.json` 可以转换。
- [ ] 旧结果 JSON 继续可读。
- [ ] 旧启动器测试继续通过。
- [ ] 测试过程不连接真实 VISA 仪器。

### E.4 快照测试

- [ ] 运行目录生成成功。
- [ ] 快照内容可以重新加载。
- [ ] 快照包含实际资源映射。
- [ ] 快照不包含模板占位字段。
- [ ] 写入失败不会覆盖已有结果。

## 8. 阶段 F：仿真仪器安全验收

目标：在没有真实设备的环境中验证配置边界和资源清理协议。

执行链路：

```text
模板加载
    -> 生成运行配置
    -> 运行配置校验
    -> 创建运行目录
    -> 写入快照
    -> 调用仿真仪器
    -> 验证 RF 和电源安全状态
```

必须覆盖：

- [ ] RF 默认关闭。
- [ ] 正常停止时 RF 关闭。
- [ ] 异常时 RF 关闭。
- [ ] 用户停止时电源正确关闭。
- [ ] 配置不完整时不执行上电。
- [ ] DUT 和驱动通道冲突时不执行上电。
- [ ] 更换电源设备或 VISA 地址后，重新填写运行映射即可运行。
- [ ] 电源上电遵循“先栅后漏”。
- [ ] 电源掉电遵循“先漏后栅”。
- [ ] 清理动作可以重复执行且不会产生二次故障。

仿真结果不能替代真实设备精度和安全验收，只用于验证控制流程。

## 9. 阶段 G：GUI 接入运行配置

目标：增加配置边界，暂不整体重写 GUI。

### G.1 GUI 运行状态

增加或接入一个运行配置对象：

```text
current_run_configuration
├── test_plan
├── run_mapping
└── validation_result
```

### G.2 测试开始前流程

- [ ] 加载测试方案。
- [ ] 填写或选择本次信号源地址。
- [ ] 填写或选择本次频谱仪地址。
- [ ] 填写或选择本次电源地址。
- [ ] 填写 DUT 电源通道。
- [ ] 填写驱动功放电源通道。
- [ ] 填写现场接线说明。
- [ ] 由操作者确认现场接线。
- [ ] 执行运行配置校验。
- [ ] 校验通过后才允许连接仪器。
- [ ] 连接成功后才允许上电或开启 RF。
- [ ] 开始测量前写入运行快照。

### G.3 必须阻止的情况

- [ ] 设备地址为空。
- [ ] 电源通道为空。
- [ ] DUT 和驱动功放重复使用通道。
- [ ] 驱动模式开启但没有驱动通道。
- [ ] 电气参数不完整。
- [ ] 现场接线未确认。
- [ ] 配置含有模板占位值。

## 10. 阶段 H：低风险真实设备验收

只有自动化测试和仿真安全测试通过后，才执行真实设备验收。

截至 2026-08-21，前三项低风险重构的自动化和仿真接入验证已完成：纯计算测试及同步/增强测量循环回归共 52 项通过，编译检查通过。全量测试仍有 2 项与工作区 `config.json` 驱动模式状态相关的既有失败，未修改现场配置或测试预期。阶段 H 的真实设备验收尚未执行，因此前三项低风险重构不能标记为全部验收完成。

### H.1 连接验收

- [ ] 使用当天现场实际设备。
- [ ] 使用当天现场实际 IP 地址。
- [ ] 使用当天现场实际电源通道。
- [ ] 验证设备可以连接。
- [ ] 不开启 RF。
- [ ] 不执行完整功率扫描。

### H.2 安全状态验收

记录以下内容：

- [ ] RF 默认状态。
- [ ] 电源初始状态。
- [ ] 现场接线确认过程。
- [ ] 普通停止后的状态。
- [ ] 窗口关闭后的状态。
- [ ] 异常退出后的状态。
- [ ] 设备连接关闭情况。

### H.3 最小测试验收

连接和清理均正常后，再进行：

- [ ] 一个或两个频率点。
- [ ] 极少功率点。
- [ ] 低功率范围。
- [ ] 明确的最大输入保护值。
- [ ] 全程记录 RF 和电源状态。

### H.4 回滚条件

出现以下任意情况，立即停用新流程并恢复旧入口：

- RF 未关闭。
- 电源未关闭。
- 上电或掉电顺序错误。
- DUT 保护未触发。
- GUI 无法正常启动或关闭。
- 运行结果无法保存。
- 运行快照与实际设备映射不一致。

## 11. 建议提交顺序

每个提交只做一个主题，便于回滚：

```text
提交 1：整理配置模板和文档
提交 2：新增配置数据模型
提交 3：新增测试方案校验
提交 4：新增运行资源映射校验
提交 5：兼容读取旧 config.json
提交 6：新增运行快照写入
提交 7：补充自动化测试
提交 8：接入 GUI 测试前配置流程
提交 9：增加仿真仪器安全测试
提交 10：低风险真实设备验收
```

每个代码提交完成后执行：

```powershell
python -m compileall -q -f -x "__pycache__|test_results" .
python -m unittest discover -s tests -v
git diff --check
```

如果项目环境已安装 pytest，可以额外执行：

```powershell
pytest -q
```

## 12. 当前立即执行项

当前不要修改测量公式、仪器 SCPI 命令或 GUI 大结构。下一轮只执行以下五项：

1. 将 `baseline/test_plan.json` 中的通道占位键改为空对象，并保留模板说明。
2. 保留 `baseline/run_mapping.json` 作为空的运行映射模板。
3. 新增 `config_models.py`。
4. 新增配置模型、测试方案校验和运行映射校验测试。
5. 在没有真实 VISA 连接的情况下完成离线验证。

当前这四项完成并通过测试后，再进入旧 `config.json` 兼容转换和运行快照写入。

## 13. 每个小步骤的完成记录

完成任何一个小步骤后，在 `重构执行.md` 记录：

```text
日期：
阶段/步骤：
提交：
涉及文件：
行为变化：
验证命令：
仿真验证：
真实设备验收：未执行 / 通过 / 失败（附记录）
结果差异：
回滚方式：
遗留问题更新：
```

阶段完成后同步更新：

- `重构基线.md` 的阶段状态和里程碑。
- `重构执行.md` 的未完成清单和遗留问题。
- 本文对应任务的完成状态。
